/*
 * This file is generated by gdbus-codegen, do not modify it.
 *
 * The license of this code is the same as for the D-Bus interface description
 * it was derived from. Note that it links to GLib, so must comply with the
 * LGPL linking clauses.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "media_player_dbus.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

#ifdef G_ENABLE_DEBUG
#define g_marshal_value_peek_boolean(v)  g_value_get_boolean (v)
#define g_marshal_value_peek_char(v)     g_value_get_schar (v)
#define g_marshal_value_peek_uchar(v)    g_value_get_uchar (v)
#define g_marshal_value_peek_int(v)      g_value_get_int (v)
#define g_marshal_value_peek_uint(v)     g_value_get_uint (v)
#define g_marshal_value_peek_long(v)     g_value_get_long (v)
#define g_marshal_value_peek_ulong(v)    g_value_get_ulong (v)
#define g_marshal_value_peek_int64(v)    g_value_get_int64 (v)
#define g_marshal_value_peek_uint64(v)   g_value_get_uint64 (v)
#define g_marshal_value_peek_enum(v)     g_value_get_enum (v)
#define g_marshal_value_peek_flags(v)    g_value_get_flags (v)
#define g_marshal_value_peek_float(v)    g_value_get_float (v)
#define g_marshal_value_peek_double(v)   g_value_get_double (v)
#define g_marshal_value_peek_string(v)   (char*) g_value_get_string (v)
#define g_marshal_value_peek_param(v)    g_value_get_param (v)
#define g_marshal_value_peek_boxed(v)    g_value_get_boxed (v)
#define g_marshal_value_peek_pointer(v)  g_value_get_pointer (v)
#define g_marshal_value_peek_object(v)   g_value_get_object (v)
#define g_marshal_value_peek_variant(v)  g_value_get_variant (v)
#else /* !G_ENABLE_DEBUG */
/* WARNING: This code accesses GValues directly, which is UNSUPPORTED API.
 *          Do not access GValues directly in your code. Instead, use the
 *          g_value_get_*() functions
 */
#define g_marshal_value_peek_boolean(v)  (v)->data[0].v_int
#define g_marshal_value_peek_char(v)     (v)->data[0].v_int
#define g_marshal_value_peek_uchar(v)    (v)->data[0].v_uint
#define g_marshal_value_peek_int(v)      (v)->data[0].v_int
#define g_marshal_value_peek_uint(v)     (v)->data[0].v_uint
#define g_marshal_value_peek_long(v)     (v)->data[0].v_long
#define g_marshal_value_peek_ulong(v)    (v)->data[0].v_ulong
#define g_marshal_value_peek_int64(v)    (v)->data[0].v_int64
#define g_marshal_value_peek_uint64(v)   (v)->data[0].v_uint64
#define g_marshal_value_peek_enum(v)     (v)->data[0].v_long
#define g_marshal_value_peek_flags(v)    (v)->data[0].v_ulong
#define g_marshal_value_peek_float(v)    (v)->data[0].v_float
#define g_marshal_value_peek_double(v)   (v)->data[0].v_double
#define g_marshal_value_peek_string(v)   (v)->data[0].v_pointer
#define g_marshal_value_peek_param(v)    (v)->data[0].v_pointer
#define g_marshal_value_peek_boxed(v)    (v)->data[0].v_pointer
#define g_marshal_value_peek_pointer(v)  (v)->data[0].v_pointer
#define g_marshal_value_peek_object(v)   (v)->data[0].v_pointer
#define g_marshal_value_peek_variant(v)  (v)->data[0].v_pointer
#endif /* !G_ENABLE_DEBUG */

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  guint use_gvariant : 1;
  guint emits_changed_signal : 1;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

static void
_g_dbus_codegen_marshal_VOID__STRING_VARIANT_BOXED (
    GClosure     *closure,
    GValue       *return_value G_GNUC_UNUSED,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint G_GNUC_UNUSED,
    void         *marshal_data)
{
  typedef void (*_GDbusCodegenMarshalVoid_StringVariantBoxedFunc)
       (void *data1,
        const gchar *arg_interface_name,
        GVariant *arg_changed_properties,
        const gchar *const *arg_invalidated_properties,
        void *data2);
  _GDbusCodegenMarshalVoid_StringVariantBoxedFunc callback;
  GCClosure *cc = (GCClosure*) closure;
  void *data1, *data2;

  g_return_if_fail (n_param_values == 4);

  if (G_CCLOSURE_SWAP_DATA (closure))
    {
      data1 = closure->data;
      data2 = g_value_peek_pointer (param_values + 0);
    }
  else
    {
      data1 = g_value_peek_pointer (param_values + 0);
      data2 = closure->data;
    }

  callback = (_GDbusCodegenMarshalVoid_StringVariantBoxedFunc)
    (marshal_data ? marshal_data : cc->callback);

  callback (data1,
            g_marshal_value_peek_string (param_values + 1),
            g_marshal_value_peek_variant (param_values + 2),
            g_marshal_value_peek_boxed (param_values + 3),
            data2);
}

static void
_g_dbus_codegen_marshal_BOOLEAN__OBJECT_STRING_STRING (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint G_GNUC_UNUSED,
    void         *marshal_data)
{
  typedef gboolean (*_GDbusCodegenMarshalBoolean_ObjectStringStringFunc)
       (void *data1,
        GDBusMethodInvocation *arg_method_invocation,
        const gchar *arg_interface_name,
        const gchar *arg_property_name,
        void *data2);
  _GDbusCodegenMarshalBoolean_ObjectStringStringFunc callback;
  GCClosure *cc = (GCClosure*) closure;
  void *data1, *data2;
  gboolean v_return;

  g_return_if_fail (return_value != NULL);
  g_return_if_fail (n_param_values == 4);

  if (G_CCLOSURE_SWAP_DATA (closure))
    {
      data1 = closure->data;
      data2 = g_value_peek_pointer (param_values + 0);
    }
  else
    {
      data1 = g_value_peek_pointer (param_values + 0);
      data2 = closure->data;
    }

  callback = (_GDbusCodegenMarshalBoolean_ObjectStringStringFunc)
    (marshal_data ? marshal_data : cc->callback);

  v_return =
    callback (data1,
              g_marshal_value_peek_object (param_values + 1),
              g_marshal_value_peek_string (param_values + 2),
              g_marshal_value_peek_string (param_values + 3),
              data2);

  g_value_set_boolean (return_value, v_return);
}

static void
_g_dbus_codegen_marshal_BOOLEAN__OBJECT_STRING (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint G_GNUC_UNUSED,
    void         *marshal_data)
{
  typedef gboolean (*_GDbusCodegenMarshalBoolean_ObjectStringFunc)
       (void *data1,
        GDBusMethodInvocation *arg_method_invocation,
        const gchar *arg_interface_name,
        void *data2);
  _GDbusCodegenMarshalBoolean_ObjectStringFunc callback;
  GCClosure *cc = (GCClosure*) closure;
  void *data1, *data2;
  gboolean v_return;

  g_return_if_fail (return_value != NULL);
  g_return_if_fail (n_param_values == 3);

  if (G_CCLOSURE_SWAP_DATA (closure))
    {
      data1 = closure->data;
      data2 = g_value_peek_pointer (param_values + 0);
    }
  else
    {
      data1 = g_value_peek_pointer (param_values + 0);
      data2 = closure->data;
    }

  callback = (_GDbusCodegenMarshalBoolean_ObjectStringFunc)
    (marshal_data ? marshal_data : cc->callback);

  v_return =
    callback (data1,
              g_marshal_value_peek_object (param_values + 1),
              g_marshal_value_peek_string (param_values + 2),
              data2);

  g_value_set_boolean (return_value, v_return);
}

static void
_g_dbus_codegen_marshal_BOOLEAN__OBJECT_STRING_STRING_VARIANT (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint G_GNUC_UNUSED,
    void         *marshal_data)
{
  typedef gboolean (*_GDbusCodegenMarshalBoolean_ObjectStringStringVariantFunc)
       (void *data1,
        GDBusMethodInvocation *arg_method_invocation,
        const gchar *arg_interface_name,
        const gchar *arg_property_name,
        GVariant *arg_value,
        void *data2);
  _GDbusCodegenMarshalBoolean_ObjectStringStringVariantFunc callback;
  GCClosure *cc = (GCClosure*) closure;
  void *data1, *data2;
  gboolean v_return;

  g_return_if_fail (return_value != NULL);
  g_return_if_fail (n_param_values == 5);

  if (G_CCLOSURE_SWAP_DATA (closure))
    {
      data1 = closure->data;
      data2 = g_value_peek_pointer (param_values + 0);
    }
  else
    {
      data1 = g_value_peek_pointer (param_values + 0);
      data2 = closure->data;
    }

  callback = (_GDbusCodegenMarshalBoolean_ObjectStringStringVariantFunc)
    (marshal_data ? marshal_data : cc->callback);

  v_return =
    callback (data1,
              g_marshal_value_peek_object (param_values + 1),
              g_marshal_value_peek_string (param_values + 2),
              g_marshal_value_peek_string (param_values + 3),
              g_marshal_value_peek_variant (param_values + 4),
              data2);

  g_value_set_boolean (return_value, v_return);
}

static void
_g_dbus_codegen_marshal_BOOLEAN__OBJECT (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint G_GNUC_UNUSED,
    void         *marshal_data)
{
  typedef gboolean (*_GDbusCodegenMarshalBoolean_ObjectFunc)
       (void *data1,
        GDBusMethodInvocation *arg_method_invocation,
        void *data2);
  _GDbusCodegenMarshalBoolean_ObjectFunc callback;
  GCClosure *cc = (GCClosure*) closure;
  void *data1, *data2;
  gboolean v_return;

  g_return_if_fail (return_value != NULL);
  g_return_if_fail (n_param_values == 2);

  if (G_CCLOSURE_SWAP_DATA (closure))
    {
      data1 = closure->data;
      data2 = g_value_peek_pointer (param_values + 0);
    }
  else
    {
      data1 = g_value_peek_pointer (param_values + 0);
      data2 = closure->data;
    }

  callback = (_GDbusCodegenMarshalBoolean_ObjectFunc)
    (marshal_data ? marshal_data : cc->callback);

  v_return =
    callback (data1,
              g_marshal_value_peek_object (param_values + 1),
              data2);

  g_value_set_boolean (return_value, v_return);
}

static void
_g_dbus_codegen_marshal_VOID__INT64 (
    GClosure     *closure,
    GValue       *return_value G_GNUC_UNUSED,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint G_GNUC_UNUSED,
    void         *marshal_data)
{
  typedef void (*_GDbusCodegenMarshalVoid_Int64Func)
       (void *data1,
        gint64 arg_Position,
        void *data2);
  _GDbusCodegenMarshalVoid_Int64Func callback;
  GCClosure *cc = (GCClosure*) closure;
  void *data1, *data2;

  g_return_if_fail (n_param_values == 2);

  if (G_CCLOSURE_SWAP_DATA (closure))
    {
      data1 = closure->data;
      data2 = g_value_peek_pointer (param_values + 0);
    }
  else
    {
      data1 = g_value_peek_pointer (param_values + 0);
      data2 = closure->data;
    }

  callback = (_GDbusCodegenMarshalVoid_Int64Func)
    (marshal_data ? marshal_data : cc->callback);

  callback (data1,
            g_marshal_value_peek_int64 (param_values + 1),
            data2);
}

static void
_g_dbus_codegen_marshal_BOOLEAN__OBJECT_INT64 (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint G_GNUC_UNUSED,
    void         *marshal_data)
{
  typedef gboolean (*_GDbusCodegenMarshalBoolean_ObjectInt64Func)
       (void *data1,
        GDBusMethodInvocation *arg_method_invocation,
        gint64 arg_Offset,
        void *data2);
  _GDbusCodegenMarshalBoolean_ObjectInt64Func callback;
  GCClosure *cc = (GCClosure*) closure;
  void *data1, *data2;
  gboolean v_return;

  g_return_if_fail (return_value != NULL);
  g_return_if_fail (n_param_values == 3);

  if (G_CCLOSURE_SWAP_DATA (closure))
    {
      data1 = closure->data;
      data2 = g_value_peek_pointer (param_values + 0);
    }
  else
    {
      data1 = g_value_peek_pointer (param_values + 0);
      data2 = closure->data;
    }

  callback = (_GDbusCodegenMarshalBoolean_ObjectInt64Func)
    (marshal_data ? marshal_data : cc->callback);

  v_return =
    callback (data1,
              g_marshal_value_peek_object (param_values + 1),
              g_marshal_value_peek_int64 (param_values + 2),
              data2);

  g_value_set_boolean (return_value, v_return);
}

static void
_g_dbus_codegen_marshal_BOOLEAN__OBJECT_STRING_INT64 (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint G_GNUC_UNUSED,
    void         *marshal_data)
{
  typedef gboolean (*_GDbusCodegenMarshalBoolean_ObjectStringInt64Func)
       (void *data1,
        GDBusMethodInvocation *arg_method_invocation,
        const gchar *arg_TrackId,
        gint64 arg_Position,
        void *data2);
  _GDbusCodegenMarshalBoolean_ObjectStringInt64Func callback;
  GCClosure *cc = (GCClosure*) closure;
  void *data1, *data2;
  gboolean v_return;

  g_return_if_fail (return_value != NULL);
  g_return_if_fail (n_param_values == 4);

  if (G_CCLOSURE_SWAP_DATA (closure))
    {
      data1 = closure->data;
      data2 = g_value_peek_pointer (param_values + 0);
    }
  else
    {
      data1 = g_value_peek_pointer (param_values + 0);
      data2 = closure->data;
    }

  callback = (_GDbusCodegenMarshalBoolean_ObjectStringInt64Func)
    (marshal_data ? marshal_data : cc->callback);

  v_return =
    callback (data1,
              g_marshal_value_peek_object (param_values + 1),
              g_marshal_value_peek_string (param_values + 2),
              g_marshal_value_peek_int64 (param_values + 3),
              data2);

  g_value_set_boolean (return_value, v_return);
}

/* ------------------------------------------------------------------------
 * Code for interface org.freedesktop.DBus.Properties
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:DbusOrgFreedesktopDBusProperties
 * @title: DbusOrgFreedesktopDBusProperties
 * @short_description: Generated C code for the org.freedesktop.DBus.Properties D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-freedesktop-DBus-Properties.top_of_page">org.freedesktop.DBus.Properties</link> D-Bus interface in C.
 */

enum
{
  DBUS__ORG_FREEDESKTOP_DBUS_PROPERTIES_PROPERTIES_CHANGED,
};

static unsigned DBUS__ORG_FREEDESKTOP_DBUS_PROPERTIES_SIGNALS[1] = { 0 };

/* ---- Introspection data for org.freedesktop.DBus.Properties ---- */

static const _ExtendedGDBusArgInfo _dbus_org_freedesktop_dbus_properties_method_info_get_IN_ARG_interface_name =
{
  {
    -1,
    (gchar *) "interface_name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_org_freedesktop_dbus_properties_method_info_get_IN_ARG_property_name =
{
  {
    -1,
    (gchar *) "property_name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_org_freedesktop_dbus_properties_method_info_get_IN_ARG_pointers[] =
{
  &_dbus_org_freedesktop_dbus_properties_method_info_get_IN_ARG_interface_name.parent_struct,
  &_dbus_org_freedesktop_dbus_properties_method_info_get_IN_ARG_property_name.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _dbus_org_freedesktop_dbus_properties_method_info_get_OUT_ARG_value =
{
  {
    -1,
    (gchar *) "value",
    (gchar *) "v",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_org_freedesktop_dbus_properties_method_info_get_OUT_ARG_pointers[] =
{
  &_dbus_org_freedesktop_dbus_properties_method_info_get_OUT_ARG_value.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _dbus_org_freedesktop_dbus_properties_method_info_get =
{
  {
    -1,
    (gchar *) "Get",
    (GDBusArgInfo **) &_dbus_org_freedesktop_dbus_properties_method_info_get_IN_ARG_pointers,
    (GDBusArgInfo **) &_dbus_org_freedesktop_dbus_properties_method_info_get_OUT_ARG_pointers,
    NULL
  },
  "handle-get",
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_org_freedesktop_dbus_properties_method_info_get_all_IN_ARG_interface_name =
{
  {
    -1,
    (gchar *) "interface_name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_org_freedesktop_dbus_properties_method_info_get_all_IN_ARG_pointers[] =
{
  &_dbus_org_freedesktop_dbus_properties_method_info_get_all_IN_ARG_interface_name.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _dbus_org_freedesktop_dbus_properties_method_info_get_all_OUT_ARG_properties =
{
  {
    -1,
    (gchar *) "properties",
    (gchar *) "a{sv}",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_org_freedesktop_dbus_properties_method_info_get_all_OUT_ARG_pointers[] =
{
  &_dbus_org_freedesktop_dbus_properties_method_info_get_all_OUT_ARG_properties.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _dbus_org_freedesktop_dbus_properties_method_info_get_all =
{
  {
    -1,
    (gchar *) "GetAll",
    (GDBusArgInfo **) &_dbus_org_freedesktop_dbus_properties_method_info_get_all_IN_ARG_pointers,
    (GDBusArgInfo **) &_dbus_org_freedesktop_dbus_properties_method_info_get_all_OUT_ARG_pointers,
    NULL
  },
  "handle-get-all",
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_org_freedesktop_dbus_properties_method_info_set_IN_ARG_interface_name =
{
  {
    -1,
    (gchar *) "interface_name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_org_freedesktop_dbus_properties_method_info_set_IN_ARG_property_name =
{
  {
    -1,
    (gchar *) "property_name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_org_freedesktop_dbus_properties_method_info_set_IN_ARG_value =
{
  {
    -1,
    (gchar *) "value",
    (gchar *) "v",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_org_freedesktop_dbus_properties_method_info_set_IN_ARG_pointers[] =
{
  &_dbus_org_freedesktop_dbus_properties_method_info_set_IN_ARG_interface_name.parent_struct,
  &_dbus_org_freedesktop_dbus_properties_method_info_set_IN_ARG_property_name.parent_struct,
  &_dbus_org_freedesktop_dbus_properties_method_info_set_IN_ARG_value.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _dbus_org_freedesktop_dbus_properties_method_info_set =
{
  {
    -1,
    (gchar *) "Set",
    (GDBusArgInfo **) &_dbus_org_freedesktop_dbus_properties_method_info_set_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-set",
  FALSE
};

static const GDBusMethodInfo * const _dbus_org_freedesktop_dbus_properties_method_info_pointers[] =
{
  &_dbus_org_freedesktop_dbus_properties_method_info_get.parent_struct,
  &_dbus_org_freedesktop_dbus_properties_method_info_get_all.parent_struct,
  &_dbus_org_freedesktop_dbus_properties_method_info_set.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _dbus_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_interface_name =
{
  {
    -1,
    (gchar *) "interface_name",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_changed_properties =
{
  {
    -1,
    (gchar *) "changed_properties",
    (gchar *) "a{sv}",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_invalidated_properties =
{
  {
    -1,
    (gchar *) "invalidated_properties",
    (gchar *) "as",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_pointers[] =
{
  &_dbus_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_interface_name.parent_struct,
  &_dbus_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_changed_properties.parent_struct,
  &_dbus_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_invalidated_properties.parent_struct,
  NULL
};

static const _ExtendedGDBusSignalInfo _dbus_org_freedesktop_dbus_properties_signal_info_properties_changed =
{
  {
    -1,
    (gchar *) "PropertiesChanged",
    (GDBusArgInfo **) &_dbus_org_freedesktop_dbus_properties_signal_info_properties_changed_ARG_pointers,
    NULL
  },
  "properties-changed"
};

static const GDBusSignalInfo * const _dbus_org_freedesktop_dbus_properties_signal_info_pointers[] =
{
  &_dbus_org_freedesktop_dbus_properties_signal_info_properties_changed.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _dbus_org_freedesktop_dbus_properties_interface_info =
{
  {
    -1,
    (gchar *) "org.freedesktop.DBus.Properties",
    (GDBusMethodInfo **) &_dbus_org_freedesktop_dbus_properties_method_info_pointers,
    (GDBusSignalInfo **) &_dbus_org_freedesktop_dbus_properties_signal_info_pointers,
    NULL,
    NULL
  },
  "org-freedesktop-dbus-properties",
};


/**
 * dbus_org_freedesktop_dbus_properties_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-freedesktop-DBus-Properties.top_of_page">org.freedesktop.DBus.Properties</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
dbus_org_freedesktop_dbus_properties_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_dbus_org_freedesktop_dbus_properties_interface_info.parent_struct;
}

/**
 * dbus_org_freedesktop_dbus_properties_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #DbusOrgFreedesktopDBusProperties interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
dbus_org_freedesktop_dbus_properties_override_properties (GObjectClass *klass G_GNUC_UNUSED, guint property_id_begin)
{
  return property_id_begin - 1;
}


inline static void
dbus_org_freedesktop_dbus_properties_signal_marshal_properties_changed (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_VOID__STRING_VARIANT_BOXED (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_org_freedesktop_dbus_properties_method_marshal_get (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT_STRING_STRING (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_org_freedesktop_dbus_properties_method_marshal_get_all (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT_STRING (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_org_freedesktop_dbus_properties_method_marshal_set (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT_STRING_STRING_VARIANT (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}


/**
 * DbusOrgFreedesktopDBusProperties:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Properties.top_of_page">org.freedesktop.DBus.Properties</link>.
 */

/**
 * DbusOrgFreedesktopDBusPropertiesIface:
 * @parent_iface: The parent interface.
 * @handle_get: Handler for the #DbusOrgFreedesktopDBusProperties::handle-get signal.
 * @handle_get_all: Handler for the #DbusOrgFreedesktopDBusProperties::handle-get-all signal.
 * @handle_set: Handler for the #DbusOrgFreedesktopDBusProperties::handle-set signal.
 * @properties_changed: Handler for the #DbusOrgFreedesktopDBusProperties::properties-changed signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Properties.top_of_page">org.freedesktop.DBus.Properties</link>.
 */

typedef DbusOrgFreedesktopDBusPropertiesIface DbusOrgFreedesktopDBusPropertiesInterface;
G_DEFINE_INTERFACE (DbusOrgFreedesktopDBusProperties, dbus_org_freedesktop_dbus_properties, G_TYPE_OBJECT)

static void
dbus_org_freedesktop_dbus_properties_default_init (DbusOrgFreedesktopDBusPropertiesIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * DbusOrgFreedesktopDBusProperties::handle-get:
   * @object: A #DbusOrgFreedesktopDBusProperties.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_interface_name: Argument passed by remote caller.
   * @arg_property_name: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Get">Get()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_org_freedesktop_dbus_properties_complete_get() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusOrgFreedesktopDBusPropertiesIface, handle_get),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_org_freedesktop_dbus_properties_method_marshal_get,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING);

  /**
   * DbusOrgFreedesktopDBusProperties::handle-get-all:
   * @object: A #DbusOrgFreedesktopDBusProperties.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_interface_name: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.GetAll">GetAll()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_org_freedesktop_dbus_properties_complete_get_all() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-all",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusOrgFreedesktopDBusPropertiesIface, handle_get_all),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_org_freedesktop_dbus_properties_method_marshal_get_all,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /**
   * DbusOrgFreedesktopDBusProperties::handle-set:
   * @object: A #DbusOrgFreedesktopDBusProperties.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_interface_name: Argument passed by remote caller.
   * @arg_property_name: Argument passed by remote caller.
   * @arg_value: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Set">Set()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_org_freedesktop_dbus_properties_complete_set() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusOrgFreedesktopDBusPropertiesIface, handle_set),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_org_freedesktop_dbus_properties_method_marshal_set,
    G_TYPE_BOOLEAN,
    4,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_STRING, G_TYPE_VARIANT);

  /* GObject signals for received D-Bus signals: */
  /**
   * DbusOrgFreedesktopDBusProperties::properties-changed:
   * @object: A #DbusOrgFreedesktopDBusProperties.
   * @arg_interface_name: Argument.
   * @arg_changed_properties: Argument.
   * @arg_invalidated_properties: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-freedesktop-DBus-Properties.PropertiesChanged">"PropertiesChanged"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  DBUS__ORG_FREEDESKTOP_DBUS_PROPERTIES_SIGNALS[DBUS__ORG_FREEDESKTOP_DBUS_PROPERTIES_PROPERTIES_CHANGED] =
    g_signal_new ("properties-changed",
      G_TYPE_FROM_INTERFACE (iface),
      G_SIGNAL_RUN_LAST,
      G_STRUCT_OFFSET (DbusOrgFreedesktopDBusPropertiesIface, properties_changed),
      NULL,
      NULL,
      dbus_org_freedesktop_dbus_properties_signal_marshal_properties_changed,
      G_TYPE_NONE,
      3, G_TYPE_STRING, G_TYPE_VARIANT, G_TYPE_STRV);

}

/**
 * dbus_org_freedesktop_dbus_properties_emit_properties_changed:
 * @object: A #DbusOrgFreedesktopDBusProperties.
 * @arg_interface_name: Argument to pass with the signal.
 * @arg_changed_properties: Argument to pass with the signal.
 * @arg_invalidated_properties: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-freedesktop-DBus-Properties.PropertiesChanged">"PropertiesChanged"</link> D-Bus signal.
 */
void
dbus_org_freedesktop_dbus_properties_emit_properties_changed (
    DbusOrgFreedesktopDBusProperties *object,
    const gchar *arg_interface_name,
    GVariant *arg_changed_properties,
    const gchar *const *arg_invalidated_properties)
{
  g_signal_emit (object, DBUS__ORG_FREEDESKTOP_DBUS_PROPERTIES_SIGNALS[DBUS__ORG_FREEDESKTOP_DBUS_PROPERTIES_PROPERTIES_CHANGED], 0, arg_interface_name, arg_changed_properties, arg_invalidated_properties);
}

/**
 * dbus_org_freedesktop_dbus_properties_call_get:
 * @proxy: A #DbusOrgFreedesktopDBusPropertiesProxy.
 * @arg_interface_name: Argument to pass with the method invocation.
 * @arg_property_name: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Get">Get()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_org_freedesktop_dbus_properties_call_get_finish() to get the result of the operation.
 *
 * See dbus_org_freedesktop_dbus_properties_call_get_sync() for the synchronous, blocking version of this method.
 */
void
dbus_org_freedesktop_dbus_properties_call_get (
    DbusOrgFreedesktopDBusProperties *proxy,
    const gchar *arg_interface_name,
    const gchar *arg_property_name,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Get",
    g_variant_new ("(ss)",
                   arg_interface_name,
                   arg_property_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_org_freedesktop_dbus_properties_call_get_finish:
 * @proxy: A #DbusOrgFreedesktopDBusPropertiesProxy.
 * @out_value: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_org_freedesktop_dbus_properties_call_get().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_org_freedesktop_dbus_properties_call_get().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_org_freedesktop_dbus_properties_call_get_finish (
    DbusOrgFreedesktopDBusProperties *proxy,
    GVariant **out_value,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@v)",
                 out_value);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_org_freedesktop_dbus_properties_call_get_sync:
 * @proxy: A #DbusOrgFreedesktopDBusPropertiesProxy.
 * @arg_interface_name: Argument to pass with the method invocation.
 * @arg_property_name: Argument to pass with the method invocation.
 * @out_value: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Get">Get()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_org_freedesktop_dbus_properties_call_get() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_org_freedesktop_dbus_properties_call_get_sync (
    DbusOrgFreedesktopDBusProperties *proxy,
    const gchar *arg_interface_name,
    const gchar *arg_property_name,
    GVariant **out_value,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Get",
    g_variant_new ("(ss)",
                   arg_interface_name,
                   arg_property_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@v)",
                 out_value);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_org_freedesktop_dbus_properties_call_get_all:
 * @proxy: A #DbusOrgFreedesktopDBusPropertiesProxy.
 * @arg_interface_name: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.GetAll">GetAll()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_org_freedesktop_dbus_properties_call_get_all_finish() to get the result of the operation.
 *
 * See dbus_org_freedesktop_dbus_properties_call_get_all_sync() for the synchronous, blocking version of this method.
 */
void
dbus_org_freedesktop_dbus_properties_call_get_all (
    DbusOrgFreedesktopDBusProperties *proxy,
    const gchar *arg_interface_name,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GetAll",
    g_variant_new ("(s)",
                   arg_interface_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_org_freedesktop_dbus_properties_call_get_all_finish:
 * @proxy: A #DbusOrgFreedesktopDBusPropertiesProxy.
 * @out_properties: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_org_freedesktop_dbus_properties_call_get_all().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_org_freedesktop_dbus_properties_call_get_all().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_org_freedesktop_dbus_properties_call_get_all_finish (
    DbusOrgFreedesktopDBusProperties *proxy,
    GVariant **out_properties,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a{sv})",
                 out_properties);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_org_freedesktop_dbus_properties_call_get_all_sync:
 * @proxy: A #DbusOrgFreedesktopDBusPropertiesProxy.
 * @arg_interface_name: Argument to pass with the method invocation.
 * @out_properties: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.GetAll">GetAll()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_org_freedesktop_dbus_properties_call_get_all() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_org_freedesktop_dbus_properties_call_get_all_sync (
    DbusOrgFreedesktopDBusProperties *proxy,
    const gchar *arg_interface_name,
    GVariant **out_properties,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GetAll",
    g_variant_new ("(s)",
                   arg_interface_name),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(@a{sv})",
                 out_properties);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_org_freedesktop_dbus_properties_call_set:
 * @proxy: A #DbusOrgFreedesktopDBusPropertiesProxy.
 * @arg_interface_name: Argument to pass with the method invocation.
 * @arg_property_name: Argument to pass with the method invocation.
 * @arg_value: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Set">Set()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_org_freedesktop_dbus_properties_call_set_finish() to get the result of the operation.
 *
 * See dbus_org_freedesktop_dbus_properties_call_set_sync() for the synchronous, blocking version of this method.
 */
void
dbus_org_freedesktop_dbus_properties_call_set (
    DbusOrgFreedesktopDBusProperties *proxy,
    const gchar *arg_interface_name,
    const gchar *arg_property_name,
    GVariant *arg_value,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Set",
    g_variant_new ("(ss@v)",
                   arg_interface_name,
                   arg_property_name,
                   arg_value),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_org_freedesktop_dbus_properties_call_set_finish:
 * @proxy: A #DbusOrgFreedesktopDBusPropertiesProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_org_freedesktop_dbus_properties_call_set().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_org_freedesktop_dbus_properties_call_set().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_org_freedesktop_dbus_properties_call_set_finish (
    DbusOrgFreedesktopDBusProperties *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_org_freedesktop_dbus_properties_call_set_sync:
 * @proxy: A #DbusOrgFreedesktopDBusPropertiesProxy.
 * @arg_interface_name: Argument to pass with the method invocation.
 * @arg_property_name: Argument to pass with the method invocation.
 * @arg_value: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Set">Set()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_org_freedesktop_dbus_properties_call_set() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_org_freedesktop_dbus_properties_call_set_sync (
    DbusOrgFreedesktopDBusProperties *proxy,
    const gchar *arg_interface_name,
    const gchar *arg_property_name,
    GVariant *arg_value,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Set",
    g_variant_new ("(ss@v)",
                   arg_interface_name,
                   arg_property_name,
                   arg_value),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_org_freedesktop_dbus_properties_complete_get:
 * @object: A #DbusOrgFreedesktopDBusProperties.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @value: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Get">Get()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_org_freedesktop_dbus_properties_complete_get (
    DbusOrgFreedesktopDBusProperties *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation,
    GVariant *value)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@v)",
                   value));
}

/**
 * dbus_org_freedesktop_dbus_properties_complete_get_all:
 * @object: A #DbusOrgFreedesktopDBusProperties.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @properties: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.GetAll">GetAll()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_org_freedesktop_dbus_properties_complete_get_all (
    DbusOrgFreedesktopDBusProperties *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation,
    GVariant *properties)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(@a{sv})",
                   properties));
}

/**
 * dbus_org_freedesktop_dbus_properties_complete_set:
 * @object: A #DbusOrgFreedesktopDBusProperties.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-DBus-Properties.Set">Set()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_org_freedesktop_dbus_properties_complete_set (
    DbusOrgFreedesktopDBusProperties *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * DbusOrgFreedesktopDBusPropertiesProxy:
 *
 * The #DbusOrgFreedesktopDBusPropertiesProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * DbusOrgFreedesktopDBusPropertiesProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #DbusOrgFreedesktopDBusPropertiesProxy.
 */

struct _DbusOrgFreedesktopDBusPropertiesProxyPrivate
{
  GData *qdata;
};

static void dbus_org_freedesktop_dbus_properties_proxy_iface_init (DbusOrgFreedesktopDBusPropertiesIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (DbusOrgFreedesktopDBusPropertiesProxy, dbus_org_freedesktop_dbus_properties_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (DbusOrgFreedesktopDBusPropertiesProxy)
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES, dbus_org_freedesktop_dbus_properties_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (DbusOrgFreedesktopDBusPropertiesProxy, dbus_org_freedesktop_dbus_properties_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES, dbus_org_freedesktop_dbus_properties_proxy_iface_init))

#endif
static void
dbus_org_freedesktop_dbus_properties_proxy_finalize (GObject *object)
{
  DbusOrgFreedesktopDBusPropertiesProxy *proxy = DBUS_ORG_FREEDESKTOP_DBUS_PROPERTIES_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (dbus_org_freedesktop_dbus_properties_proxy_parent_class)->finalize (object);
}

static void
dbus_org_freedesktop_dbus_properties_proxy_get_property (GObject      *object G_GNUC_UNUSED,
  guint         prop_id G_GNUC_UNUSED,
  GValue       *value G_GNUC_UNUSED,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
dbus_org_freedesktop_dbus_properties_proxy_set_property (GObject      *object G_GNUC_UNUSED,
  guint         prop_id G_GNUC_UNUSED,
  const GValue *value G_GNUC_UNUSED,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
dbus_org_freedesktop_dbus_properties_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_dbus_org_freedesktop_dbus_properties_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
dbus_org_freedesktop_dbus_properties_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  DbusOrgFreedesktopDBusPropertiesProxy *proxy = DBUS_ORG_FREEDESKTOP_DBUS_PROPERTIES_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_org_freedesktop_dbus_properties_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_org_freedesktop_dbus_properties_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
dbus_org_freedesktop_dbus_properties_proxy_init (DbusOrgFreedesktopDBusPropertiesProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = dbus_org_freedesktop_dbus_properties_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES_PROXY, DbusOrgFreedesktopDBusPropertiesProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), dbus_org_freedesktop_dbus_properties_interface_info ());
}

static void
dbus_org_freedesktop_dbus_properties_proxy_class_init (DbusOrgFreedesktopDBusPropertiesProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = dbus_org_freedesktop_dbus_properties_proxy_finalize;
  gobject_class->get_property = dbus_org_freedesktop_dbus_properties_proxy_get_property;
  gobject_class->set_property = dbus_org_freedesktop_dbus_properties_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = dbus_org_freedesktop_dbus_properties_proxy_g_signal;
  proxy_class->g_properties_changed = dbus_org_freedesktop_dbus_properties_proxy_g_properties_changed;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (DbusOrgFreedesktopDBusPropertiesProxyPrivate));
#endif
}

static void
dbus_org_freedesktop_dbus_properties_proxy_iface_init (DbusOrgFreedesktopDBusPropertiesIface *iface G_GNUC_UNUSED)
{
}

/**
 * dbus_org_freedesktop_dbus_properties_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Properties.top_of_page">org.freedesktop.DBus.Properties</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_org_freedesktop_dbus_properties_proxy_new_finish() to get the result of the operation.
 *
 * See dbus_org_freedesktop_dbus_properties_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
dbus_org_freedesktop_dbus_properties_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Properties", NULL);
}

/**
 * dbus_org_freedesktop_dbus_properties_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_org_freedesktop_dbus_properties_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with dbus_org_freedesktop_dbus_properties_proxy_new().
 *
 * Returns: (transfer full) (type DbusOrgFreedesktopDBusPropertiesProxy): The constructed proxy object or %NULL if @error is set.
 */
DbusOrgFreedesktopDBusProperties *
dbus_org_freedesktop_dbus_properties_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return DBUS_ORG_FREEDESKTOP_DBUS_PROPERTIES (ret);
  else
    return NULL;
}

/**
 * dbus_org_freedesktop_dbus_properties_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Properties.top_of_page">org.freedesktop.DBus.Properties</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See dbus_org_freedesktop_dbus_properties_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type DbusOrgFreedesktopDBusPropertiesProxy): The constructed proxy object or %NULL if @error is set.
 */
DbusOrgFreedesktopDBusProperties *
dbus_org_freedesktop_dbus_properties_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Properties", NULL);
  if (ret != NULL)
    return DBUS_ORG_FREEDESKTOP_DBUS_PROPERTIES (ret);
  else
    return NULL;
}


/**
 * dbus_org_freedesktop_dbus_properties_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like dbus_org_freedesktop_dbus_properties_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_org_freedesktop_dbus_properties_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See dbus_org_freedesktop_dbus_properties_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
dbus_org_freedesktop_dbus_properties_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Properties", NULL);
}

/**
 * dbus_org_freedesktop_dbus_properties_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_org_freedesktop_dbus_properties_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with dbus_org_freedesktop_dbus_properties_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type DbusOrgFreedesktopDBusPropertiesProxy): The constructed proxy object or %NULL if @error is set.
 */
DbusOrgFreedesktopDBusProperties *
dbus_org_freedesktop_dbus_properties_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return DBUS_ORG_FREEDESKTOP_DBUS_PROPERTIES (ret);
  else
    return NULL;
}

/**
 * dbus_org_freedesktop_dbus_properties_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like dbus_org_freedesktop_dbus_properties_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See dbus_org_freedesktop_dbus_properties_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type DbusOrgFreedesktopDBusPropertiesProxy): The constructed proxy object or %NULL if @error is set.
 */
DbusOrgFreedesktopDBusProperties *
dbus_org_freedesktop_dbus_properties_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Properties", NULL);
  if (ret != NULL)
    return DBUS_ORG_FREEDESKTOP_DBUS_PROPERTIES (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * DbusOrgFreedesktopDBusPropertiesSkeleton:
 *
 * The #DbusOrgFreedesktopDBusPropertiesSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * DbusOrgFreedesktopDBusPropertiesSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #DbusOrgFreedesktopDBusPropertiesSkeleton.
 */

struct _DbusOrgFreedesktopDBusPropertiesSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_dbus_org_freedesktop_dbus_properties_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  DbusOrgFreedesktopDBusPropertiesSkeleton *skeleton = DBUS_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_dbus_org_freedesktop_dbus_properties_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  DbusOrgFreedesktopDBusPropertiesSkeleton *skeleton = DBUS_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_org_freedesktop_dbus_properties_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_dbus_org_freedesktop_dbus_properties_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  DbusOrgFreedesktopDBusPropertiesSkeleton *skeleton = DBUS_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_org_freedesktop_dbus_properties_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _dbus_org_freedesktop_dbus_properties_skeleton_vtable =
{
  _dbus_org_freedesktop_dbus_properties_skeleton_handle_method_call,
  _dbus_org_freedesktop_dbus_properties_skeleton_handle_get_property,
  _dbus_org_freedesktop_dbus_properties_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
dbus_org_freedesktop_dbus_properties_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return dbus_org_freedesktop_dbus_properties_interface_info ();
}

static GDBusInterfaceVTable *
dbus_org_freedesktop_dbus_properties_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_dbus_org_freedesktop_dbus_properties_skeleton_vtable;
}

static GVariant *
dbus_org_freedesktop_dbus_properties_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  DbusOrgFreedesktopDBusPropertiesSkeleton *skeleton = DBUS_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_dbus_org_freedesktop_dbus_properties_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _dbus_org_freedesktop_dbus_properties_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _dbus_org_freedesktop_dbus_properties_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _dbus_org_freedesktop_dbus_properties_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.DBus.Properties", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
dbus_org_freedesktop_dbus_properties_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton G_GNUC_UNUSED)
{
}

static void
_dbus_org_freedesktop_dbus_properties_on_signal_properties_changed (
    DbusOrgFreedesktopDBusProperties *object,
    const gchar *arg_interface_name,
    GVariant *arg_changed_properties,
    const gchar *const *arg_invalidated_properties)
{
  DbusOrgFreedesktopDBusPropertiesSkeleton *skeleton = DBUS_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(s@a{sv}^as)",
                   arg_interface_name,
                   arg_changed_properties,
                   arg_invalidated_properties));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.DBus.Properties", "PropertiesChanged",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void dbus_org_freedesktop_dbus_properties_skeleton_iface_init (DbusOrgFreedesktopDBusPropertiesIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (DbusOrgFreedesktopDBusPropertiesSkeleton, dbus_org_freedesktop_dbus_properties_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (DbusOrgFreedesktopDBusPropertiesSkeleton)
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES, dbus_org_freedesktop_dbus_properties_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (DbusOrgFreedesktopDBusPropertiesSkeleton, dbus_org_freedesktop_dbus_properties_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES, dbus_org_freedesktop_dbus_properties_skeleton_iface_init))

#endif
static void
dbus_org_freedesktop_dbus_properties_skeleton_finalize (GObject *object)
{
  DbusOrgFreedesktopDBusPropertiesSkeleton *skeleton = DBUS_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (dbus_org_freedesktop_dbus_properties_skeleton_parent_class)->finalize (object);
}

static void
dbus_org_freedesktop_dbus_properties_skeleton_init (DbusOrgFreedesktopDBusPropertiesSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = dbus_org_freedesktop_dbus_properties_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON, DbusOrgFreedesktopDBusPropertiesSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
dbus_org_freedesktop_dbus_properties_skeleton_class_init (DbusOrgFreedesktopDBusPropertiesSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = dbus_org_freedesktop_dbus_properties_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = dbus_org_freedesktop_dbus_properties_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = dbus_org_freedesktop_dbus_properties_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = dbus_org_freedesktop_dbus_properties_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = dbus_org_freedesktop_dbus_properties_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (DbusOrgFreedesktopDBusPropertiesSkeletonPrivate));
#endif
}

static void
dbus_org_freedesktop_dbus_properties_skeleton_iface_init (DbusOrgFreedesktopDBusPropertiesIface *iface)
{
  iface->properties_changed = _dbus_org_freedesktop_dbus_properties_on_signal_properties_changed;
}

/**
 * dbus_org_freedesktop_dbus_properties_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Properties.top_of_page">org.freedesktop.DBus.Properties</link>.
 *
 * Returns: (transfer full) (type DbusOrgFreedesktopDBusPropertiesSkeleton): The skeleton object.
 */
DbusOrgFreedesktopDBusProperties *
dbus_org_freedesktop_dbus_properties_skeleton_new (void)
{
  return DBUS_ORG_FREEDESKTOP_DBUS_PROPERTIES (g_object_new (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PROPERTIES_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.freedesktop.DBus.Introspectable
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:DbusOrgFreedesktopDBusIntrospectable
 * @title: DbusOrgFreedesktopDBusIntrospectable
 * @short_description: Generated C code for the org.freedesktop.DBus.Introspectable D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-freedesktop-DBus-Introspectable.top_of_page">org.freedesktop.DBus.Introspectable</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.freedesktop.DBus.Introspectable ---- */

static const _ExtendedGDBusArgInfo _dbus_org_freedesktop_dbus_introspectable_method_info_introspect_OUT_ARG_xml_data =
{
  {
    -1,
    (gchar *) "xml_data",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_org_freedesktop_dbus_introspectable_method_info_introspect_OUT_ARG_pointers[] =
{
  &_dbus_org_freedesktop_dbus_introspectable_method_info_introspect_OUT_ARG_xml_data.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _dbus_org_freedesktop_dbus_introspectable_method_info_introspect =
{
  {
    -1,
    (gchar *) "Introspect",
    NULL,
    (GDBusArgInfo **) &_dbus_org_freedesktop_dbus_introspectable_method_info_introspect_OUT_ARG_pointers,
    NULL
  },
  "handle-introspect",
  FALSE
};

static const GDBusMethodInfo * const _dbus_org_freedesktop_dbus_introspectable_method_info_pointers[] =
{
  &_dbus_org_freedesktop_dbus_introspectable_method_info_introspect.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _dbus_org_freedesktop_dbus_introspectable_interface_info =
{
  {
    -1,
    (gchar *) "org.freedesktop.DBus.Introspectable",
    (GDBusMethodInfo **) &_dbus_org_freedesktop_dbus_introspectable_method_info_pointers,
    NULL,
    NULL,
    NULL
  },
  "org-freedesktop-dbus-introspectable",
};


/**
 * dbus_org_freedesktop_dbus_introspectable_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-freedesktop-DBus-Introspectable.top_of_page">org.freedesktop.DBus.Introspectable</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
dbus_org_freedesktop_dbus_introspectable_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_dbus_org_freedesktop_dbus_introspectable_interface_info.parent_struct;
}

/**
 * dbus_org_freedesktop_dbus_introspectable_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #DbusOrgFreedesktopDBusIntrospectable interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
dbus_org_freedesktop_dbus_introspectable_override_properties (GObjectClass *klass G_GNUC_UNUSED, guint property_id_begin)
{
  return property_id_begin - 1;
}


inline static void
dbus_org_freedesktop_dbus_introspectable_method_marshal_introspect (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}


/**
 * DbusOrgFreedesktopDBusIntrospectable:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Introspectable.top_of_page">org.freedesktop.DBus.Introspectable</link>.
 */

/**
 * DbusOrgFreedesktopDBusIntrospectableIface:
 * @parent_iface: The parent interface.
 * @handle_introspect: Handler for the #DbusOrgFreedesktopDBusIntrospectable::handle-introspect signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Introspectable.top_of_page">org.freedesktop.DBus.Introspectable</link>.
 */

typedef DbusOrgFreedesktopDBusIntrospectableIface DbusOrgFreedesktopDBusIntrospectableInterface;
G_DEFINE_INTERFACE (DbusOrgFreedesktopDBusIntrospectable, dbus_org_freedesktop_dbus_introspectable, G_TYPE_OBJECT)

static void
dbus_org_freedesktop_dbus_introspectable_default_init (DbusOrgFreedesktopDBusIntrospectableIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * DbusOrgFreedesktopDBusIntrospectable::handle-introspect:
   * @object: A #DbusOrgFreedesktopDBusIntrospectable.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-DBus-Introspectable.Introspect">Introspect()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_org_freedesktop_dbus_introspectable_complete_introspect() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-introspect",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusOrgFreedesktopDBusIntrospectableIface, handle_introspect),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_org_freedesktop_dbus_introspectable_method_marshal_introspect,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

}

/**
 * dbus_org_freedesktop_dbus_introspectable_call_introspect:
 * @proxy: A #DbusOrgFreedesktopDBusIntrospectableProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Introspectable.Introspect">Introspect()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_org_freedesktop_dbus_introspectable_call_introspect_finish() to get the result of the operation.
 *
 * See dbus_org_freedesktop_dbus_introspectable_call_introspect_sync() for the synchronous, blocking version of this method.
 */
void
dbus_org_freedesktop_dbus_introspectable_call_introspect (
    DbusOrgFreedesktopDBusIntrospectable *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Introspect",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_org_freedesktop_dbus_introspectable_call_introspect_finish:
 * @proxy: A #DbusOrgFreedesktopDBusIntrospectableProxy.
 * @out_xml_data: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_org_freedesktop_dbus_introspectable_call_introspect().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_org_freedesktop_dbus_introspectable_call_introspect().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_org_freedesktop_dbus_introspectable_call_introspect_finish (
    DbusOrgFreedesktopDBusIntrospectable *proxy,
    gchar **out_xml_data,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_xml_data);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_org_freedesktop_dbus_introspectable_call_introspect_sync:
 * @proxy: A #DbusOrgFreedesktopDBusIntrospectableProxy.
 * @out_xml_data: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Introspectable.Introspect">Introspect()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_org_freedesktop_dbus_introspectable_call_introspect() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_org_freedesktop_dbus_introspectable_call_introspect_sync (
    DbusOrgFreedesktopDBusIntrospectable *proxy,
    gchar **out_xml_data,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Introspect",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_xml_data);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_org_freedesktop_dbus_introspectable_complete_introspect:
 * @object: A #DbusOrgFreedesktopDBusIntrospectable.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @xml_data: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-DBus-Introspectable.Introspect">Introspect()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_org_freedesktop_dbus_introspectable_complete_introspect (
    DbusOrgFreedesktopDBusIntrospectable *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation,
    const gchar *xml_data)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(s)",
                   xml_data));
}

/* ------------------------------------------------------------------------ */

/**
 * DbusOrgFreedesktopDBusIntrospectableProxy:
 *
 * The #DbusOrgFreedesktopDBusIntrospectableProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * DbusOrgFreedesktopDBusIntrospectableProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #DbusOrgFreedesktopDBusIntrospectableProxy.
 */

struct _DbusOrgFreedesktopDBusIntrospectableProxyPrivate
{
  GData *qdata;
};

static void dbus_org_freedesktop_dbus_introspectable_proxy_iface_init (DbusOrgFreedesktopDBusIntrospectableIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (DbusOrgFreedesktopDBusIntrospectableProxy, dbus_org_freedesktop_dbus_introspectable_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (DbusOrgFreedesktopDBusIntrospectableProxy)
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE, dbus_org_freedesktop_dbus_introspectable_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (DbusOrgFreedesktopDBusIntrospectableProxy, dbus_org_freedesktop_dbus_introspectable_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE, dbus_org_freedesktop_dbus_introspectable_proxy_iface_init))

#endif
static void
dbus_org_freedesktop_dbus_introspectable_proxy_finalize (GObject *object)
{
  DbusOrgFreedesktopDBusIntrospectableProxy *proxy = DBUS_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (dbus_org_freedesktop_dbus_introspectable_proxy_parent_class)->finalize (object);
}

static void
dbus_org_freedesktop_dbus_introspectable_proxy_get_property (GObject      *object G_GNUC_UNUSED,
  guint         prop_id G_GNUC_UNUSED,
  GValue       *value G_GNUC_UNUSED,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
dbus_org_freedesktop_dbus_introspectable_proxy_set_property (GObject      *object G_GNUC_UNUSED,
  guint         prop_id G_GNUC_UNUSED,
  const GValue *value G_GNUC_UNUSED,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
dbus_org_freedesktop_dbus_introspectable_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_dbus_org_freedesktop_dbus_introspectable_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], DBUS_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, DBUS_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
dbus_org_freedesktop_dbus_introspectable_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  DbusOrgFreedesktopDBusIntrospectableProxy *proxy = DBUS_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_org_freedesktop_dbus_introspectable_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_org_freedesktop_dbus_introspectable_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
dbus_org_freedesktop_dbus_introspectable_proxy_init (DbusOrgFreedesktopDBusIntrospectableProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = dbus_org_freedesktop_dbus_introspectable_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, DBUS_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_PROXY, DbusOrgFreedesktopDBusIntrospectableProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), dbus_org_freedesktop_dbus_introspectable_interface_info ());
}

static void
dbus_org_freedesktop_dbus_introspectable_proxy_class_init (DbusOrgFreedesktopDBusIntrospectableProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = dbus_org_freedesktop_dbus_introspectable_proxy_finalize;
  gobject_class->get_property = dbus_org_freedesktop_dbus_introspectable_proxy_get_property;
  gobject_class->set_property = dbus_org_freedesktop_dbus_introspectable_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = dbus_org_freedesktop_dbus_introspectable_proxy_g_signal;
  proxy_class->g_properties_changed = dbus_org_freedesktop_dbus_introspectable_proxy_g_properties_changed;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (DbusOrgFreedesktopDBusIntrospectableProxyPrivate));
#endif
}

static void
dbus_org_freedesktop_dbus_introspectable_proxy_iface_init (DbusOrgFreedesktopDBusIntrospectableIface *iface G_GNUC_UNUSED)
{
}

/**
 * dbus_org_freedesktop_dbus_introspectable_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Introspectable.top_of_page">org.freedesktop.DBus.Introspectable</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_org_freedesktop_dbus_introspectable_proxy_new_finish() to get the result of the operation.
 *
 * See dbus_org_freedesktop_dbus_introspectable_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
dbus_org_freedesktop_dbus_introspectable_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Introspectable", NULL);
}

/**
 * dbus_org_freedesktop_dbus_introspectable_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_org_freedesktop_dbus_introspectable_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with dbus_org_freedesktop_dbus_introspectable_proxy_new().
 *
 * Returns: (transfer full) (type DbusOrgFreedesktopDBusIntrospectableProxy): The constructed proxy object or %NULL if @error is set.
 */
DbusOrgFreedesktopDBusIntrospectable *
dbus_org_freedesktop_dbus_introspectable_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return DBUS_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE (ret);
  else
    return NULL;
}

/**
 * dbus_org_freedesktop_dbus_introspectable_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Introspectable.top_of_page">org.freedesktop.DBus.Introspectable</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See dbus_org_freedesktop_dbus_introspectable_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type DbusOrgFreedesktopDBusIntrospectableProxy): The constructed proxy object or %NULL if @error is set.
 */
DbusOrgFreedesktopDBusIntrospectable *
dbus_org_freedesktop_dbus_introspectable_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Introspectable", NULL);
  if (ret != NULL)
    return DBUS_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE (ret);
  else
    return NULL;
}


/**
 * dbus_org_freedesktop_dbus_introspectable_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like dbus_org_freedesktop_dbus_introspectable_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_org_freedesktop_dbus_introspectable_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See dbus_org_freedesktop_dbus_introspectable_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
dbus_org_freedesktop_dbus_introspectable_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Introspectable", NULL);
}

/**
 * dbus_org_freedesktop_dbus_introspectable_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_org_freedesktop_dbus_introspectable_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with dbus_org_freedesktop_dbus_introspectable_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type DbusOrgFreedesktopDBusIntrospectableProxy): The constructed proxy object or %NULL if @error is set.
 */
DbusOrgFreedesktopDBusIntrospectable *
dbus_org_freedesktop_dbus_introspectable_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return DBUS_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE (ret);
  else
    return NULL;
}

/**
 * dbus_org_freedesktop_dbus_introspectable_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like dbus_org_freedesktop_dbus_introspectable_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See dbus_org_freedesktop_dbus_introspectable_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type DbusOrgFreedesktopDBusIntrospectableProxy): The constructed proxy object or %NULL if @error is set.
 */
DbusOrgFreedesktopDBusIntrospectable *
dbus_org_freedesktop_dbus_introspectable_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Introspectable", NULL);
  if (ret != NULL)
    return DBUS_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * DbusOrgFreedesktopDBusIntrospectableSkeleton:
 *
 * The #DbusOrgFreedesktopDBusIntrospectableSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * DbusOrgFreedesktopDBusIntrospectableSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #DbusOrgFreedesktopDBusIntrospectableSkeleton.
 */

struct _DbusOrgFreedesktopDBusIntrospectableSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_dbus_org_freedesktop_dbus_introspectable_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  DbusOrgFreedesktopDBusIntrospectableSkeleton *skeleton = DBUS_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], DBUS_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, DBUS_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_dbus_org_freedesktop_dbus_introspectable_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  DbusOrgFreedesktopDBusIntrospectableSkeleton *skeleton = DBUS_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_org_freedesktop_dbus_introspectable_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_dbus_org_freedesktop_dbus_introspectable_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  DbusOrgFreedesktopDBusIntrospectableSkeleton *skeleton = DBUS_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_org_freedesktop_dbus_introspectable_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _dbus_org_freedesktop_dbus_introspectable_skeleton_vtable =
{
  _dbus_org_freedesktop_dbus_introspectable_skeleton_handle_method_call,
  _dbus_org_freedesktop_dbus_introspectable_skeleton_handle_get_property,
  _dbus_org_freedesktop_dbus_introspectable_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
dbus_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return dbus_org_freedesktop_dbus_introspectable_interface_info ();
}

static GDBusInterfaceVTable *
dbus_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_dbus_org_freedesktop_dbus_introspectable_skeleton_vtable;
}

static GVariant *
dbus_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  DbusOrgFreedesktopDBusIntrospectableSkeleton *skeleton = DBUS_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_dbus_org_freedesktop_dbus_introspectable_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _dbus_org_freedesktop_dbus_introspectable_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _dbus_org_freedesktop_dbus_introspectable_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _dbus_org_freedesktop_dbus_introspectable_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.DBus.Introspectable", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
dbus_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton G_GNUC_UNUSED)
{
}

static void dbus_org_freedesktop_dbus_introspectable_skeleton_iface_init (DbusOrgFreedesktopDBusIntrospectableIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (DbusOrgFreedesktopDBusIntrospectableSkeleton, dbus_org_freedesktop_dbus_introspectable_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (DbusOrgFreedesktopDBusIntrospectableSkeleton)
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE, dbus_org_freedesktop_dbus_introspectable_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (DbusOrgFreedesktopDBusIntrospectableSkeleton, dbus_org_freedesktop_dbus_introspectable_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE, dbus_org_freedesktop_dbus_introspectable_skeleton_iface_init))

#endif
static void
dbus_org_freedesktop_dbus_introspectable_skeleton_finalize (GObject *object)
{
  DbusOrgFreedesktopDBusIntrospectableSkeleton *skeleton = DBUS_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (dbus_org_freedesktop_dbus_introspectable_skeleton_parent_class)->finalize (object);
}

static void
dbus_org_freedesktop_dbus_introspectable_skeleton_init (DbusOrgFreedesktopDBusIntrospectableSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = dbus_org_freedesktop_dbus_introspectable_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, DBUS_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_SKELETON, DbusOrgFreedesktopDBusIntrospectableSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
dbus_org_freedesktop_dbus_introspectable_skeleton_class_init (DbusOrgFreedesktopDBusIntrospectableSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = dbus_org_freedesktop_dbus_introspectable_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = dbus_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = dbus_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = dbus_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = dbus_org_freedesktop_dbus_introspectable_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (DbusOrgFreedesktopDBusIntrospectableSkeletonPrivate));
#endif
}

static void
dbus_org_freedesktop_dbus_introspectable_skeleton_iface_init (DbusOrgFreedesktopDBusIntrospectableIface *iface G_GNUC_UNUSED)
{
}

/**
 * dbus_org_freedesktop_dbus_introspectable_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Introspectable.top_of_page">org.freedesktop.DBus.Introspectable</link>.
 *
 * Returns: (transfer full) (type DbusOrgFreedesktopDBusIntrospectableSkeleton): The skeleton object.
 */
DbusOrgFreedesktopDBusIntrospectable *
dbus_org_freedesktop_dbus_introspectable_skeleton_new (void)
{
  return DBUS_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE (g_object_new (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_INTROSPECTABLE_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.freedesktop.DBus.Peer
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:DbusOrgFreedesktopDBusPeer
 * @title: DbusOrgFreedesktopDBusPeer
 * @short_description: Generated C code for the org.freedesktop.DBus.Peer D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-freedesktop-DBus-Peer.top_of_page">org.freedesktop.DBus.Peer</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.freedesktop.DBus.Peer ---- */

static const _ExtendedGDBusMethodInfo _dbus_org_freedesktop_dbus_peer_method_info_ping =
{
  {
    -1,
    (gchar *) "Ping",
    NULL,
    NULL,
    NULL
  },
  "handle-ping",
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_org_freedesktop_dbus_peer_method_info_get_machine_id_OUT_ARG_machine_uuid =
{
  {
    -1,
    (gchar *) "machine_uuid",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_org_freedesktop_dbus_peer_method_info_get_machine_id_OUT_ARG_pointers[] =
{
  &_dbus_org_freedesktop_dbus_peer_method_info_get_machine_id_OUT_ARG_machine_uuid.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _dbus_org_freedesktop_dbus_peer_method_info_get_machine_id =
{
  {
    -1,
    (gchar *) "GetMachineId",
    NULL,
    (GDBusArgInfo **) &_dbus_org_freedesktop_dbus_peer_method_info_get_machine_id_OUT_ARG_pointers,
    NULL
  },
  "handle-get-machine-id",
  FALSE
};

static const GDBusMethodInfo * const _dbus_org_freedesktop_dbus_peer_method_info_pointers[] =
{
  &_dbus_org_freedesktop_dbus_peer_method_info_ping.parent_struct,
  &_dbus_org_freedesktop_dbus_peer_method_info_get_machine_id.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _dbus_org_freedesktop_dbus_peer_interface_info =
{
  {
    -1,
    (gchar *) "org.freedesktop.DBus.Peer",
    (GDBusMethodInfo **) &_dbus_org_freedesktop_dbus_peer_method_info_pointers,
    NULL,
    NULL,
    NULL
  },
  "org-freedesktop-dbus-peer",
};


/**
 * dbus_org_freedesktop_dbus_peer_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-freedesktop-DBus-Peer.top_of_page">org.freedesktop.DBus.Peer</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
dbus_org_freedesktop_dbus_peer_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_dbus_org_freedesktop_dbus_peer_interface_info.parent_struct;
}

/**
 * dbus_org_freedesktop_dbus_peer_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #DbusOrgFreedesktopDBusPeer interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
dbus_org_freedesktop_dbus_peer_override_properties (GObjectClass *klass G_GNUC_UNUSED, guint property_id_begin)
{
  return property_id_begin - 1;
}


inline static void
dbus_org_freedesktop_dbus_peer_method_marshal_ping (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_org_freedesktop_dbus_peer_method_marshal_get_machine_id (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}


/**
 * DbusOrgFreedesktopDBusPeer:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Peer.top_of_page">org.freedesktop.DBus.Peer</link>.
 */

/**
 * DbusOrgFreedesktopDBusPeerIface:
 * @parent_iface: The parent interface.
 * @handle_get_machine_id: Handler for the #DbusOrgFreedesktopDBusPeer::handle-get-machine-id signal.
 * @handle_ping: Handler for the #DbusOrgFreedesktopDBusPeer::handle-ping signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Peer.top_of_page">org.freedesktop.DBus.Peer</link>.
 */

typedef DbusOrgFreedesktopDBusPeerIface DbusOrgFreedesktopDBusPeerInterface;
G_DEFINE_INTERFACE (DbusOrgFreedesktopDBusPeer, dbus_org_freedesktop_dbus_peer, G_TYPE_OBJECT)

static void
dbus_org_freedesktop_dbus_peer_default_init (DbusOrgFreedesktopDBusPeerIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * DbusOrgFreedesktopDBusPeer::handle-ping:
   * @object: A #DbusOrgFreedesktopDBusPeer.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-DBus-Peer.Ping">Ping()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_org_freedesktop_dbus_peer_complete_ping() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-ping",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusOrgFreedesktopDBusPeerIface, handle_ping),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_org_freedesktop_dbus_peer_method_marshal_ping,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * DbusOrgFreedesktopDBusPeer::handle-get-machine-id:
   * @object: A #DbusOrgFreedesktopDBusPeer.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-freedesktop-DBus-Peer.GetMachineId">GetMachineId()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_org_freedesktop_dbus_peer_complete_get_machine_id() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-get-machine-id",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusOrgFreedesktopDBusPeerIface, handle_get_machine_id),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_org_freedesktop_dbus_peer_method_marshal_get_machine_id,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

}

/**
 * dbus_org_freedesktop_dbus_peer_call_ping:
 * @proxy: A #DbusOrgFreedesktopDBusPeerProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Peer.Ping">Ping()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_org_freedesktop_dbus_peer_call_ping_finish() to get the result of the operation.
 *
 * See dbus_org_freedesktop_dbus_peer_call_ping_sync() for the synchronous, blocking version of this method.
 */
void
dbus_org_freedesktop_dbus_peer_call_ping (
    DbusOrgFreedesktopDBusPeer *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Ping",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_org_freedesktop_dbus_peer_call_ping_finish:
 * @proxy: A #DbusOrgFreedesktopDBusPeerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_org_freedesktop_dbus_peer_call_ping().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_org_freedesktop_dbus_peer_call_ping().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_org_freedesktop_dbus_peer_call_ping_finish (
    DbusOrgFreedesktopDBusPeer *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_org_freedesktop_dbus_peer_call_ping_sync:
 * @proxy: A #DbusOrgFreedesktopDBusPeerProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Peer.Ping">Ping()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_org_freedesktop_dbus_peer_call_ping() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_org_freedesktop_dbus_peer_call_ping_sync (
    DbusOrgFreedesktopDBusPeer *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Ping",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_org_freedesktop_dbus_peer_call_get_machine_id:
 * @proxy: A #DbusOrgFreedesktopDBusPeerProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Peer.GetMachineId">GetMachineId()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_org_freedesktop_dbus_peer_call_get_machine_id_finish() to get the result of the operation.
 *
 * See dbus_org_freedesktop_dbus_peer_call_get_machine_id_sync() for the synchronous, blocking version of this method.
 */
void
dbus_org_freedesktop_dbus_peer_call_get_machine_id (
    DbusOrgFreedesktopDBusPeer *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "GetMachineId",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_org_freedesktop_dbus_peer_call_get_machine_id_finish:
 * @proxy: A #DbusOrgFreedesktopDBusPeerProxy.
 * @out_machine_uuid: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_org_freedesktop_dbus_peer_call_get_machine_id().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_org_freedesktop_dbus_peer_call_get_machine_id().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_org_freedesktop_dbus_peer_call_get_machine_id_finish (
    DbusOrgFreedesktopDBusPeer *proxy,
    gchar **out_machine_uuid,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_machine_uuid);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_org_freedesktop_dbus_peer_call_get_machine_id_sync:
 * @proxy: A #DbusOrgFreedesktopDBusPeerProxy.
 * @out_machine_uuid: (out) (optional): Return location for return parameter or %NULL to ignore.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-freedesktop-DBus-Peer.GetMachineId">GetMachineId()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_org_freedesktop_dbus_peer_call_get_machine_id() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_org_freedesktop_dbus_peer_call_get_machine_id_sync (
    DbusOrgFreedesktopDBusPeer *proxy,
    gchar **out_machine_uuid,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "GetMachineId",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "(s)",
                 out_machine_uuid);
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_org_freedesktop_dbus_peer_complete_ping:
 * @object: A #DbusOrgFreedesktopDBusPeer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-DBus-Peer.Ping">Ping()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_org_freedesktop_dbus_peer_complete_ping (
    DbusOrgFreedesktopDBusPeer *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * dbus_org_freedesktop_dbus_peer_complete_get_machine_id:
 * @object: A #DbusOrgFreedesktopDBusPeer.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 * @machine_uuid: Parameter to return.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-freedesktop-DBus-Peer.GetMachineId">GetMachineId()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_org_freedesktop_dbus_peer_complete_get_machine_id (
    DbusOrgFreedesktopDBusPeer *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation,
    const gchar *machine_uuid)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("(s)",
                   machine_uuid));
}

/* ------------------------------------------------------------------------ */

/**
 * DbusOrgFreedesktopDBusPeerProxy:
 *
 * The #DbusOrgFreedesktopDBusPeerProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * DbusOrgFreedesktopDBusPeerProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #DbusOrgFreedesktopDBusPeerProxy.
 */

struct _DbusOrgFreedesktopDBusPeerProxyPrivate
{
  GData *qdata;
};

static void dbus_org_freedesktop_dbus_peer_proxy_iface_init (DbusOrgFreedesktopDBusPeerIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (DbusOrgFreedesktopDBusPeerProxy, dbus_org_freedesktop_dbus_peer_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (DbusOrgFreedesktopDBusPeerProxy)
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PEER, dbus_org_freedesktop_dbus_peer_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (DbusOrgFreedesktopDBusPeerProxy, dbus_org_freedesktop_dbus_peer_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PEER, dbus_org_freedesktop_dbus_peer_proxy_iface_init))

#endif
static void
dbus_org_freedesktop_dbus_peer_proxy_finalize (GObject *object)
{
  DbusOrgFreedesktopDBusPeerProxy *proxy = DBUS_ORG_FREEDESKTOP_DBUS_PEER_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (dbus_org_freedesktop_dbus_peer_proxy_parent_class)->finalize (object);
}

static void
dbus_org_freedesktop_dbus_peer_proxy_get_property (GObject      *object G_GNUC_UNUSED,
  guint         prop_id G_GNUC_UNUSED,
  GValue       *value G_GNUC_UNUSED,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
dbus_org_freedesktop_dbus_peer_proxy_set_property (GObject      *object G_GNUC_UNUSED,
  guint         prop_id G_GNUC_UNUSED,
  const GValue *value G_GNUC_UNUSED,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
}

static void
dbus_org_freedesktop_dbus_peer_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_dbus_org_freedesktop_dbus_peer_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PEER);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PEER);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
dbus_org_freedesktop_dbus_peer_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  DbusOrgFreedesktopDBusPeerProxy *proxy = DBUS_ORG_FREEDESKTOP_DBUS_PEER_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_org_freedesktop_dbus_peer_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_org_freedesktop_dbus_peer_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static void
dbus_org_freedesktop_dbus_peer_proxy_init (DbusOrgFreedesktopDBusPeerProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = dbus_org_freedesktop_dbus_peer_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PEER_PROXY, DbusOrgFreedesktopDBusPeerProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), dbus_org_freedesktop_dbus_peer_interface_info ());
}

static void
dbus_org_freedesktop_dbus_peer_proxy_class_init (DbusOrgFreedesktopDBusPeerProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = dbus_org_freedesktop_dbus_peer_proxy_finalize;
  gobject_class->get_property = dbus_org_freedesktop_dbus_peer_proxy_get_property;
  gobject_class->set_property = dbus_org_freedesktop_dbus_peer_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = dbus_org_freedesktop_dbus_peer_proxy_g_signal;
  proxy_class->g_properties_changed = dbus_org_freedesktop_dbus_peer_proxy_g_properties_changed;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (DbusOrgFreedesktopDBusPeerProxyPrivate));
#endif
}

static void
dbus_org_freedesktop_dbus_peer_proxy_iface_init (DbusOrgFreedesktopDBusPeerIface *iface G_GNUC_UNUSED)
{
}

/**
 * dbus_org_freedesktop_dbus_peer_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Peer.top_of_page">org.freedesktop.DBus.Peer</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_org_freedesktop_dbus_peer_proxy_new_finish() to get the result of the operation.
 *
 * See dbus_org_freedesktop_dbus_peer_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
dbus_org_freedesktop_dbus_peer_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PEER_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Peer", NULL);
}

/**
 * dbus_org_freedesktop_dbus_peer_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_org_freedesktop_dbus_peer_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with dbus_org_freedesktop_dbus_peer_proxy_new().
 *
 * Returns: (transfer full) (type DbusOrgFreedesktopDBusPeerProxy): The constructed proxy object or %NULL if @error is set.
 */
DbusOrgFreedesktopDBusPeer *
dbus_org_freedesktop_dbus_peer_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return DBUS_ORG_FREEDESKTOP_DBUS_PEER (ret);
  else
    return NULL;
}

/**
 * dbus_org_freedesktop_dbus_peer_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Peer.top_of_page">org.freedesktop.DBus.Peer</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See dbus_org_freedesktop_dbus_peer_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type DbusOrgFreedesktopDBusPeerProxy): The constructed proxy object or %NULL if @error is set.
 */
DbusOrgFreedesktopDBusPeer *
dbus_org_freedesktop_dbus_peer_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PEER_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Peer", NULL);
  if (ret != NULL)
    return DBUS_ORG_FREEDESKTOP_DBUS_PEER (ret);
  else
    return NULL;
}


/**
 * dbus_org_freedesktop_dbus_peer_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like dbus_org_freedesktop_dbus_peer_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_org_freedesktop_dbus_peer_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See dbus_org_freedesktop_dbus_peer_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
dbus_org_freedesktop_dbus_peer_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PEER_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Peer", NULL);
}

/**
 * dbus_org_freedesktop_dbus_peer_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_org_freedesktop_dbus_peer_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with dbus_org_freedesktop_dbus_peer_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type DbusOrgFreedesktopDBusPeerProxy): The constructed proxy object or %NULL if @error is set.
 */
DbusOrgFreedesktopDBusPeer *
dbus_org_freedesktop_dbus_peer_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return DBUS_ORG_FREEDESKTOP_DBUS_PEER (ret);
  else
    return NULL;
}

/**
 * dbus_org_freedesktop_dbus_peer_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like dbus_org_freedesktop_dbus_peer_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See dbus_org_freedesktop_dbus_peer_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type DbusOrgFreedesktopDBusPeerProxy): The constructed proxy object or %NULL if @error is set.
 */
DbusOrgFreedesktopDBusPeer *
dbus_org_freedesktop_dbus_peer_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PEER_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.freedesktop.DBus.Peer", NULL);
  if (ret != NULL)
    return DBUS_ORG_FREEDESKTOP_DBUS_PEER (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * DbusOrgFreedesktopDBusPeerSkeleton:
 *
 * The #DbusOrgFreedesktopDBusPeerSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * DbusOrgFreedesktopDBusPeerSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #DbusOrgFreedesktopDBusPeerSkeleton.
 */

struct _DbusOrgFreedesktopDBusPeerSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_dbus_org_freedesktop_dbus_peer_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  DbusOrgFreedesktopDBusPeerSkeleton *skeleton = DBUS_ORG_FREEDESKTOP_DBUS_PEER_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PEER);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PEER);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_dbus_org_freedesktop_dbus_peer_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  DbusOrgFreedesktopDBusPeerSkeleton *skeleton = DBUS_ORG_FREEDESKTOP_DBUS_PEER_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_org_freedesktop_dbus_peer_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_dbus_org_freedesktop_dbus_peer_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  DbusOrgFreedesktopDBusPeerSkeleton *skeleton = DBUS_ORG_FREEDESKTOP_DBUS_PEER_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_org_freedesktop_dbus_peer_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _dbus_org_freedesktop_dbus_peer_skeleton_vtable =
{
  _dbus_org_freedesktop_dbus_peer_skeleton_handle_method_call,
  _dbus_org_freedesktop_dbus_peer_skeleton_handle_get_property,
  _dbus_org_freedesktop_dbus_peer_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
dbus_org_freedesktop_dbus_peer_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return dbus_org_freedesktop_dbus_peer_interface_info ();
}

static GDBusInterfaceVTable *
dbus_org_freedesktop_dbus_peer_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_dbus_org_freedesktop_dbus_peer_skeleton_vtable;
}

static GVariant *
dbus_org_freedesktop_dbus_peer_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  DbusOrgFreedesktopDBusPeerSkeleton *skeleton = DBUS_ORG_FREEDESKTOP_DBUS_PEER_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_dbus_org_freedesktop_dbus_peer_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _dbus_org_freedesktop_dbus_peer_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _dbus_org_freedesktop_dbus_peer_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _dbus_org_freedesktop_dbus_peer_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.freedesktop.DBus.Peer", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static void
dbus_org_freedesktop_dbus_peer_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton G_GNUC_UNUSED)
{
}

static void dbus_org_freedesktop_dbus_peer_skeleton_iface_init (DbusOrgFreedesktopDBusPeerIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (DbusOrgFreedesktopDBusPeerSkeleton, dbus_org_freedesktop_dbus_peer_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (DbusOrgFreedesktopDBusPeerSkeleton)
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PEER, dbus_org_freedesktop_dbus_peer_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (DbusOrgFreedesktopDBusPeerSkeleton, dbus_org_freedesktop_dbus_peer_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PEER, dbus_org_freedesktop_dbus_peer_skeleton_iface_init))

#endif
static void
dbus_org_freedesktop_dbus_peer_skeleton_finalize (GObject *object)
{
  DbusOrgFreedesktopDBusPeerSkeleton *skeleton = DBUS_ORG_FREEDESKTOP_DBUS_PEER_SKELETON (object);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (dbus_org_freedesktop_dbus_peer_skeleton_parent_class)->finalize (object);
}

static void
dbus_org_freedesktop_dbus_peer_skeleton_init (DbusOrgFreedesktopDBusPeerSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = dbus_org_freedesktop_dbus_peer_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PEER_SKELETON, DbusOrgFreedesktopDBusPeerSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
}

static void
dbus_org_freedesktop_dbus_peer_skeleton_class_init (DbusOrgFreedesktopDBusPeerSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = dbus_org_freedesktop_dbus_peer_skeleton_finalize;

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = dbus_org_freedesktop_dbus_peer_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = dbus_org_freedesktop_dbus_peer_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = dbus_org_freedesktop_dbus_peer_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = dbus_org_freedesktop_dbus_peer_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (DbusOrgFreedesktopDBusPeerSkeletonPrivate));
#endif
}

static void
dbus_org_freedesktop_dbus_peer_skeleton_iface_init (DbusOrgFreedesktopDBusPeerIface *iface G_GNUC_UNUSED)
{
}

/**
 * dbus_org_freedesktop_dbus_peer_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-freedesktop-DBus-Peer.top_of_page">org.freedesktop.DBus.Peer</link>.
 *
 * Returns: (transfer full) (type DbusOrgFreedesktopDBusPeerSkeleton): The skeleton object.
 */
DbusOrgFreedesktopDBusPeer *
dbus_org_freedesktop_dbus_peer_skeleton_new (void)
{
  return DBUS_ORG_FREEDESKTOP_DBUS_PEER (g_object_new (DBUS_TYPE_ORG_FREEDESKTOP_DBUS_PEER_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.mpris.MediaPlayer2
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:DbusMediaPlayer2
 * @title: DbusMediaPlayer2
 * @short_description: Generated C code for the org.mpris.MediaPlayer2 D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-mpris-MediaPlayer2.top_of_page">org.mpris.MediaPlayer2</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.mpris.MediaPlayer2 ---- */

static const _ExtendedGDBusMethodInfo _dbus_media_player2_method_info_raise =
{
  {
    -1,
    (gchar *) "Raise",
    NULL,
    NULL,
    NULL
  },
  "handle-raise",
  FALSE
};

static const _ExtendedGDBusMethodInfo _dbus_media_player2_method_info_quit =
{
  {
    -1,
    (gchar *) "Quit",
    NULL,
    NULL,
    NULL
  },
  "handle-quit",
  FALSE
};

static const GDBusMethodInfo * const _dbus_media_player2_method_info_pointers[] =
{
  &_dbus_media_player2_method_info_raise.parent_struct,
  &_dbus_media_player2_method_info_quit.parent_struct,
  NULL
};

static const _ExtendedGDBusPropertyInfo _dbus_media_player2_property_info_can_quit =
{
  {
    -1,
    (gchar *) "CanQuit",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "can-quit",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _dbus_media_player2_property_info_can_set_fullscreen =
{
  {
    -1,
    (gchar *) "CanSetFullscreen",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "can-set-fullscreen",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _dbus_media_player2_property_info_can_raise =
{
  {
    -1,
    (gchar *) "CanRaise",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "can-raise",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _dbus_media_player2_property_info_has_track_list =
{
  {
    -1,
    (gchar *) "HasTrackList",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "has-track-list",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _dbus_media_player2_property_info_identity =
{
  {
    -1,
    (gchar *) "Identity",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "identity",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _dbus_media_player2_property_info_desktop_entry =
{
  {
    -1,
    (gchar *) "DesktopEntry",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "desktop-entry",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _dbus_media_player2_property_info_supported_uri_schemes =
{
  {
    -1,
    (gchar *) "SupportedUriSchemes",
    (gchar *) "as",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "supported-uri-schemes",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _dbus_media_player2_property_info_supported_mime_types =
{
  {
    -1,
    (gchar *) "SupportedMimeTypes",
    (gchar *) "as",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "supported-mime-types",
  FALSE,
  TRUE
};

static const GDBusPropertyInfo * const _dbus_media_player2_property_info_pointers[] =
{
  &_dbus_media_player2_property_info_can_quit.parent_struct,
  &_dbus_media_player2_property_info_can_set_fullscreen.parent_struct,
  &_dbus_media_player2_property_info_can_raise.parent_struct,
  &_dbus_media_player2_property_info_has_track_list.parent_struct,
  &_dbus_media_player2_property_info_identity.parent_struct,
  &_dbus_media_player2_property_info_desktop_entry.parent_struct,
  &_dbus_media_player2_property_info_supported_uri_schemes.parent_struct,
  &_dbus_media_player2_property_info_supported_mime_types.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _dbus_media_player2_interface_info =
{
  {
    -1,
    (gchar *) "org.mpris.MediaPlayer2",
    (GDBusMethodInfo **) &_dbus_media_player2_method_info_pointers,
    NULL,
    (GDBusPropertyInfo **) &_dbus_media_player2_property_info_pointers,
    NULL
  },
  "media-player2",
};


/**
 * dbus_media_player2_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-mpris-MediaPlayer2.top_of_page">org.mpris.MediaPlayer2</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
dbus_media_player2_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_dbus_media_player2_interface_info.parent_struct;
}

/**
 * dbus_media_player2_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #DbusMediaPlayer2 interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
dbus_media_player2_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "can-quit");
  g_object_class_override_property (klass, property_id_begin++, "can-set-fullscreen");
  g_object_class_override_property (klass, property_id_begin++, "can-raise");
  g_object_class_override_property (klass, property_id_begin++, "has-track-list");
  g_object_class_override_property (klass, property_id_begin++, "identity");
  g_object_class_override_property (klass, property_id_begin++, "desktop-entry");
  g_object_class_override_property (klass, property_id_begin++, "supported-uri-schemes");
  g_object_class_override_property (klass, property_id_begin++, "supported-mime-types");
  return property_id_begin - 1;
}


inline static void
dbus_media_player2_method_marshal_raise (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_media_player2_method_marshal_quit (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}


/**
 * DbusMediaPlayer2:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2.top_of_page">org.mpris.MediaPlayer2</link>.
 */

/**
 * DbusMediaPlayer2Iface:
 * @parent_iface: The parent interface.
 * @handle_quit: Handler for the #DbusMediaPlayer2::handle-quit signal.
 * @handle_raise: Handler for the #DbusMediaPlayer2::handle-raise signal.
 * @get_can_quit: Getter for the #DbusMediaPlayer2:can-quit property.
 * @get_can_raise: Getter for the #DbusMediaPlayer2:can-raise property.
 * @get_can_set_fullscreen: Getter for the #DbusMediaPlayer2:can-set-fullscreen property.
 * @get_desktop_entry: Getter for the #DbusMediaPlayer2:desktop-entry property.
 * @get_has_track_list: Getter for the #DbusMediaPlayer2:has-track-list property.
 * @get_identity: Getter for the #DbusMediaPlayer2:identity property.
 * @get_supported_mime_types: Getter for the #DbusMediaPlayer2:supported-mime-types property.
 * @get_supported_uri_schemes: Getter for the #DbusMediaPlayer2:supported-uri-schemes property.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2.top_of_page">org.mpris.MediaPlayer2</link>.
 */

typedef DbusMediaPlayer2Iface DbusMediaPlayer2Interface;
G_DEFINE_INTERFACE (DbusMediaPlayer2, dbus_media_player2, G_TYPE_OBJECT)

static void
dbus_media_player2_default_init (DbusMediaPlayer2Iface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * DbusMediaPlayer2::handle-raise:
   * @object: A #DbusMediaPlayer2.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2.Raise">Raise()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_media_player2_complete_raise() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-raise",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusMediaPlayer2Iface, handle_raise),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_media_player2_method_marshal_raise,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * DbusMediaPlayer2::handle-quit:
   * @object: A #DbusMediaPlayer2.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2.Quit">Quit()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_media_player2_complete_quit() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-quit",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusMediaPlayer2Iface, handle_quit),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_media_player2_method_marshal_quit,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /* GObject properties for D-Bus properties: */
  /**
   * DbusMediaPlayer2:can-quit:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2.CanQuit">"CanQuit"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("can-quit", "CanQuit", "CanQuit", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * DbusMediaPlayer2:can-set-fullscreen:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2.CanSetFullscreen">"CanSetFullscreen"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("can-set-fullscreen", "CanSetFullscreen", "CanSetFullscreen", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * DbusMediaPlayer2:can-raise:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2.CanRaise">"CanRaise"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("can-raise", "CanRaise", "CanRaise", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * DbusMediaPlayer2:has-track-list:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2.HasTrackList">"HasTrackList"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("has-track-list", "HasTrackList", "HasTrackList", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * DbusMediaPlayer2:identity:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2.Identity">"Identity"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("identity", "Identity", "Identity", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * DbusMediaPlayer2:desktop-entry:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2.DesktopEntry">"DesktopEntry"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("desktop-entry", "DesktopEntry", "DesktopEntry", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * DbusMediaPlayer2:supported-uri-schemes:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2.SupportedUriSchemes">"SupportedUriSchemes"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("supported-uri-schemes", "SupportedUriSchemes", "SupportedUriSchemes", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * DbusMediaPlayer2:supported-mime-types:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2.SupportedMimeTypes">"SupportedMimeTypes"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boxed ("supported-mime-types", "SupportedMimeTypes", "SupportedMimeTypes", G_TYPE_STRV, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * dbus_media_player2_get_can_quit: (skip)
 * @object: A #DbusMediaPlayer2.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.CanQuit">"CanQuit"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
dbus_media_player2_get_can_quit (DbusMediaPlayer2 *object)
{
  g_return_val_if_fail (DBUS_IS_MEDIA_PLAYER2 (object), FALSE);

  return DBUS_MEDIA_PLAYER2_GET_IFACE (object)->get_can_quit (object);
}

/**
 * dbus_media_player2_set_can_quit: (skip)
 * @object: A #DbusMediaPlayer2.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2.CanQuit">"CanQuit"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
dbus_media_player2_set_can_quit (DbusMediaPlayer2 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "can-quit", value, NULL);
}

/**
 * dbus_media_player2_get_can_set_fullscreen: (skip)
 * @object: A #DbusMediaPlayer2.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.CanSetFullscreen">"CanSetFullscreen"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
dbus_media_player2_get_can_set_fullscreen (DbusMediaPlayer2 *object)
{
  g_return_val_if_fail (DBUS_IS_MEDIA_PLAYER2 (object), FALSE);

  return DBUS_MEDIA_PLAYER2_GET_IFACE (object)->get_can_set_fullscreen (object);
}

/**
 * dbus_media_player2_set_can_set_fullscreen: (skip)
 * @object: A #DbusMediaPlayer2.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2.CanSetFullscreen">"CanSetFullscreen"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
dbus_media_player2_set_can_set_fullscreen (DbusMediaPlayer2 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "can-set-fullscreen", value, NULL);
}

/**
 * dbus_media_player2_get_can_raise: (skip)
 * @object: A #DbusMediaPlayer2.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.CanRaise">"CanRaise"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
dbus_media_player2_get_can_raise (DbusMediaPlayer2 *object)
{
  g_return_val_if_fail (DBUS_IS_MEDIA_PLAYER2 (object), FALSE);

  return DBUS_MEDIA_PLAYER2_GET_IFACE (object)->get_can_raise (object);
}

/**
 * dbus_media_player2_set_can_raise: (skip)
 * @object: A #DbusMediaPlayer2.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2.CanRaise">"CanRaise"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
dbus_media_player2_set_can_raise (DbusMediaPlayer2 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "can-raise", value, NULL);
}

/**
 * dbus_media_player2_get_has_track_list: (skip)
 * @object: A #DbusMediaPlayer2.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.HasTrackList">"HasTrackList"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
dbus_media_player2_get_has_track_list (DbusMediaPlayer2 *object)
{
  g_return_val_if_fail (DBUS_IS_MEDIA_PLAYER2 (object), FALSE);

  return DBUS_MEDIA_PLAYER2_GET_IFACE (object)->get_has_track_list (object);
}

/**
 * dbus_media_player2_set_has_track_list: (skip)
 * @object: A #DbusMediaPlayer2.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2.HasTrackList">"HasTrackList"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
dbus_media_player2_set_has_track_list (DbusMediaPlayer2 *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "has-track-list", value, NULL);
}

/**
 * dbus_media_player2_get_identity: (skip)
 * @object: A #DbusMediaPlayer2.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.Identity">"Identity"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use dbus_media_player2_dup_identity() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
dbus_media_player2_get_identity (DbusMediaPlayer2 *object)
{
  g_return_val_if_fail (DBUS_IS_MEDIA_PLAYER2 (object), NULL);

  return DBUS_MEDIA_PLAYER2_GET_IFACE (object)->get_identity (object);
}

/**
 * dbus_media_player2_dup_identity: (skip)
 * @object: A #DbusMediaPlayer2.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.Identity">"Identity"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
dbus_media_player2_dup_identity (DbusMediaPlayer2 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "identity", &value, NULL);
  return value;
}

/**
 * dbus_media_player2_set_identity: (skip)
 * @object: A #DbusMediaPlayer2.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2.Identity">"Identity"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
dbus_media_player2_set_identity (DbusMediaPlayer2 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "identity", value, NULL);
}

/**
 * dbus_media_player2_get_desktop_entry: (skip)
 * @object: A #DbusMediaPlayer2.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.DesktopEntry">"DesktopEntry"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use dbus_media_player2_dup_desktop_entry() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
dbus_media_player2_get_desktop_entry (DbusMediaPlayer2 *object)
{
  g_return_val_if_fail (DBUS_IS_MEDIA_PLAYER2 (object), NULL);

  return DBUS_MEDIA_PLAYER2_GET_IFACE (object)->get_desktop_entry (object);
}

/**
 * dbus_media_player2_dup_desktop_entry: (skip)
 * @object: A #DbusMediaPlayer2.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.DesktopEntry">"DesktopEntry"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
dbus_media_player2_dup_desktop_entry (DbusMediaPlayer2 *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "desktop-entry", &value, NULL);
  return value;
}

/**
 * dbus_media_player2_set_desktop_entry: (skip)
 * @object: A #DbusMediaPlayer2.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2.DesktopEntry">"DesktopEntry"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
dbus_media_player2_set_desktop_entry (DbusMediaPlayer2 *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "desktop-entry", value, NULL);
}

/**
 * dbus_media_player2_get_supported_uri_schemes: (skip)
 * @object: A #DbusMediaPlayer2.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.SupportedUriSchemes">"SupportedUriSchemes"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use dbus_media_player2_dup_supported_uri_schemes() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
dbus_media_player2_get_supported_uri_schemes (DbusMediaPlayer2 *object)
{
  g_return_val_if_fail (DBUS_IS_MEDIA_PLAYER2 (object), NULL);

  return DBUS_MEDIA_PLAYER2_GET_IFACE (object)->get_supported_uri_schemes (object);
}

/**
 * dbus_media_player2_dup_supported_uri_schemes: (skip)
 * @object: A #DbusMediaPlayer2.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.SupportedUriSchemes">"SupportedUriSchemes"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
dbus_media_player2_dup_supported_uri_schemes (DbusMediaPlayer2 *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "supported-uri-schemes", &value, NULL);
  return value;
}

/**
 * dbus_media_player2_set_supported_uri_schemes: (skip)
 * @object: A #DbusMediaPlayer2.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2.SupportedUriSchemes">"SupportedUriSchemes"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
dbus_media_player2_set_supported_uri_schemes (DbusMediaPlayer2 *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "supported-uri-schemes", value, NULL);
}

/**
 * dbus_media_player2_get_supported_mime_types: (skip)
 * @object: A #DbusMediaPlayer2.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.SupportedMimeTypes">"SupportedMimeTypes"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use dbus_media_player2_dup_supported_mime_types() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *const *
dbus_media_player2_get_supported_mime_types (DbusMediaPlayer2 *object)
{
  g_return_val_if_fail (DBUS_IS_MEDIA_PLAYER2 (object), NULL);

  return DBUS_MEDIA_PLAYER2_GET_IFACE (object)->get_supported_mime_types (object);
}

/**
 * dbus_media_player2_dup_supported_mime_types: (skip)
 * @object: A #DbusMediaPlayer2.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-mpris-MediaPlayer2.SupportedMimeTypes">"SupportedMimeTypes"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_strfreev().
 */
gchar **
dbus_media_player2_dup_supported_mime_types (DbusMediaPlayer2 *object)
{
  gchar **value;
  g_object_get (G_OBJECT (object), "supported-mime-types", &value, NULL);
  return value;
}

/**
 * dbus_media_player2_set_supported_mime_types: (skip)
 * @object: A #DbusMediaPlayer2.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2.SupportedMimeTypes">"SupportedMimeTypes"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
dbus_media_player2_set_supported_mime_types (DbusMediaPlayer2 *object, const gchar *const *value)
{
  g_object_set (G_OBJECT (object), "supported-mime-types", value, NULL);
}

/**
 * dbus_media_player2_call_raise:
 * @proxy: A #DbusMediaPlayer2Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2.Raise">Raise()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_media_player2_call_raise_finish() to get the result of the operation.
 *
 * See dbus_media_player2_call_raise_sync() for the synchronous, blocking version of this method.
 */
void
dbus_media_player2_call_raise (
    DbusMediaPlayer2 *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Raise",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_media_player2_call_raise_finish:
 * @proxy: A #DbusMediaPlayer2Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_media_player2_call_raise().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_media_player2_call_raise().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_media_player2_call_raise_finish (
    DbusMediaPlayer2 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_media_player2_call_raise_sync:
 * @proxy: A #DbusMediaPlayer2Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2.Raise">Raise()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_media_player2_call_raise() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_media_player2_call_raise_sync (
    DbusMediaPlayer2 *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Raise",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_media_player2_call_quit:
 * @proxy: A #DbusMediaPlayer2Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2.Quit">Quit()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_media_player2_call_quit_finish() to get the result of the operation.
 *
 * See dbus_media_player2_call_quit_sync() for the synchronous, blocking version of this method.
 */
void
dbus_media_player2_call_quit (
    DbusMediaPlayer2 *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Quit",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_media_player2_call_quit_finish:
 * @proxy: A #DbusMediaPlayer2Proxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_media_player2_call_quit().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_media_player2_call_quit().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_media_player2_call_quit_finish (
    DbusMediaPlayer2 *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_media_player2_call_quit_sync:
 * @proxy: A #DbusMediaPlayer2Proxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2.Quit">Quit()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_media_player2_call_quit() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_media_player2_call_quit_sync (
    DbusMediaPlayer2 *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Quit",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_media_player2_complete_raise:
 * @object: A #DbusMediaPlayer2.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2.Raise">Raise()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_media_player2_complete_raise (
    DbusMediaPlayer2 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * dbus_media_player2_complete_quit:
 * @object: A #DbusMediaPlayer2.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2.Quit">Quit()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_media_player2_complete_quit (
    DbusMediaPlayer2 *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * DbusMediaPlayer2Proxy:
 *
 * The #DbusMediaPlayer2Proxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * DbusMediaPlayer2ProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #DbusMediaPlayer2Proxy.
 */

struct _DbusMediaPlayer2ProxyPrivate
{
  GData *qdata;
};

static void dbus_media_player2_proxy_iface_init (DbusMediaPlayer2Iface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (DbusMediaPlayer2Proxy, dbus_media_player2_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (DbusMediaPlayer2Proxy)
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_MEDIA_PLAYER2, dbus_media_player2_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (DbusMediaPlayer2Proxy, dbus_media_player2_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_MEDIA_PLAYER2, dbus_media_player2_proxy_iface_init))

#endif
static void
dbus_media_player2_proxy_finalize (GObject *object)
{
  DbusMediaPlayer2Proxy *proxy = DBUS_MEDIA_PLAYER2_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (dbus_media_player2_proxy_parent_class)->finalize (object);
}

static void
dbus_media_player2_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 8);
  info = (const _ExtendedGDBusPropertyInfo *) _dbus_media_player2_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
dbus_media_player2_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface org.mpris.MediaPlayer2: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
dbus_media_player2_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 8);
  info = (const _ExtendedGDBusPropertyInfo *) _dbus_media_player2_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.mpris.MediaPlayer2", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) dbus_media_player2_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
dbus_media_player2_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_dbus_media_player2_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], DBUS_TYPE_MEDIA_PLAYER2);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, DBUS_TYPE_MEDIA_PLAYER2);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
dbus_media_player2_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  DbusMediaPlayer2Proxy *proxy = DBUS_MEDIA_PLAYER2_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_media_player2_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_media_player2_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static gboolean 
dbus_media_player2_proxy_get_can_quit (DbusMediaPlayer2 *object)
{
  DbusMediaPlayer2Proxy *proxy = DBUS_MEDIA_PLAYER2_PROXY (object);
  GVariant *variant;
  gboolean value = FALSE;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "CanQuit");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
dbus_media_player2_proxy_get_can_set_fullscreen (DbusMediaPlayer2 *object)
{
  DbusMediaPlayer2Proxy *proxy = DBUS_MEDIA_PLAYER2_PROXY (object);
  GVariant *variant;
  gboolean value = FALSE;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "CanSetFullscreen");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
dbus_media_player2_proxy_get_can_raise (DbusMediaPlayer2 *object)
{
  DbusMediaPlayer2Proxy *proxy = DBUS_MEDIA_PLAYER2_PROXY (object);
  GVariant *variant;
  gboolean value = FALSE;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "CanRaise");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
dbus_media_player2_proxy_get_has_track_list (DbusMediaPlayer2 *object)
{
  DbusMediaPlayer2Proxy *proxy = DBUS_MEDIA_PLAYER2_PROXY (object);
  GVariant *variant;
  gboolean value = FALSE;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "HasTrackList");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
dbus_media_player2_proxy_get_identity (DbusMediaPlayer2 *object)
{
  DbusMediaPlayer2Proxy *proxy = DBUS_MEDIA_PLAYER2_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Identity");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
dbus_media_player2_proxy_get_desktop_entry (DbusMediaPlayer2 *object)
{
  DbusMediaPlayer2Proxy *proxy = DBUS_MEDIA_PLAYER2_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "DesktopEntry");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *const *
dbus_media_player2_proxy_get_supported_uri_schemes (DbusMediaPlayer2 *object)
{
  DbusMediaPlayer2Proxy *proxy = DBUS_MEDIA_PLAYER2_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  value = g_datalist_get_data (&proxy->priv->qdata, "SupportedUriSchemes");
  if (value != NULL)
    return value;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "SupportedUriSchemes");
  if (variant != NULL)
    {
      value = g_variant_get_strv (variant, NULL);
      g_datalist_set_data_full (&proxy->priv->qdata, "SupportedUriSchemes", (gpointer) value, g_free);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *const *
dbus_media_player2_proxy_get_supported_mime_types (DbusMediaPlayer2 *object)
{
  DbusMediaPlayer2Proxy *proxy = DBUS_MEDIA_PLAYER2_PROXY (object);
  GVariant *variant;
  const gchar *const *value = NULL;
  value = g_datalist_get_data (&proxy->priv->qdata, "SupportedMimeTypes");
  if (value != NULL)
    return value;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "SupportedMimeTypes");
  if (variant != NULL)
    {
      value = g_variant_get_strv (variant, NULL);
      g_datalist_set_data_full (&proxy->priv->qdata, "SupportedMimeTypes", (gpointer) value, g_free);
      g_variant_unref (variant);
    }
  return value;
}

static void
dbus_media_player2_proxy_init (DbusMediaPlayer2Proxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = dbus_media_player2_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, DBUS_TYPE_MEDIA_PLAYER2_PROXY, DbusMediaPlayer2ProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), dbus_media_player2_interface_info ());
}

static void
dbus_media_player2_proxy_class_init (DbusMediaPlayer2ProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = dbus_media_player2_proxy_finalize;
  gobject_class->get_property = dbus_media_player2_proxy_get_property;
  gobject_class->set_property = dbus_media_player2_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = dbus_media_player2_proxy_g_signal;
  proxy_class->g_properties_changed = dbus_media_player2_proxy_g_properties_changed;

  dbus_media_player2_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (DbusMediaPlayer2ProxyPrivate));
#endif
}

static void
dbus_media_player2_proxy_iface_init (DbusMediaPlayer2Iface *iface)
{
  iface->get_can_quit = dbus_media_player2_proxy_get_can_quit;
  iface->get_can_set_fullscreen = dbus_media_player2_proxy_get_can_set_fullscreen;
  iface->get_can_raise = dbus_media_player2_proxy_get_can_raise;
  iface->get_has_track_list = dbus_media_player2_proxy_get_has_track_list;
  iface->get_identity = dbus_media_player2_proxy_get_identity;
  iface->get_desktop_entry = dbus_media_player2_proxy_get_desktop_entry;
  iface->get_supported_uri_schemes = dbus_media_player2_proxy_get_supported_uri_schemes;
  iface->get_supported_mime_types = dbus_media_player2_proxy_get_supported_mime_types;
}

/**
 * dbus_media_player2_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2.top_of_page">org.mpris.MediaPlayer2</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_media_player2_proxy_new_finish() to get the result of the operation.
 *
 * See dbus_media_player2_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
dbus_media_player2_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (DBUS_TYPE_MEDIA_PLAYER2_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2", NULL);
}

/**
 * dbus_media_player2_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_media_player2_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with dbus_media_player2_proxy_new().
 *
 * Returns: (transfer full) (type DbusMediaPlayer2Proxy): The constructed proxy object or %NULL if @error is set.
 */
DbusMediaPlayer2 *
dbus_media_player2_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return DBUS_MEDIA_PLAYER2 (ret);
  else
    return NULL;
}

/**
 * dbus_media_player2_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2.top_of_page">org.mpris.MediaPlayer2</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See dbus_media_player2_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type DbusMediaPlayer2Proxy): The constructed proxy object or %NULL if @error is set.
 */
DbusMediaPlayer2 *
dbus_media_player2_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (DBUS_TYPE_MEDIA_PLAYER2_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2", NULL);
  if (ret != NULL)
    return DBUS_MEDIA_PLAYER2 (ret);
  else
    return NULL;
}


/**
 * dbus_media_player2_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like dbus_media_player2_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_media_player2_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See dbus_media_player2_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
dbus_media_player2_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (DBUS_TYPE_MEDIA_PLAYER2_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2", NULL);
}

/**
 * dbus_media_player2_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_media_player2_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with dbus_media_player2_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type DbusMediaPlayer2Proxy): The constructed proxy object or %NULL if @error is set.
 */
DbusMediaPlayer2 *
dbus_media_player2_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return DBUS_MEDIA_PLAYER2 (ret);
  else
    return NULL;
}

/**
 * dbus_media_player2_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like dbus_media_player2_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See dbus_media_player2_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type DbusMediaPlayer2Proxy): The constructed proxy object or %NULL if @error is set.
 */
DbusMediaPlayer2 *
dbus_media_player2_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (DBUS_TYPE_MEDIA_PLAYER2_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2", NULL);
  if (ret != NULL)
    return DBUS_MEDIA_PLAYER2 (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * DbusMediaPlayer2Skeleton:
 *
 * The #DbusMediaPlayer2Skeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * DbusMediaPlayer2SkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #DbusMediaPlayer2Skeleton.
 */

struct _DbusMediaPlayer2SkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_dbus_media_player2_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  DbusMediaPlayer2Skeleton *skeleton = DBUS_MEDIA_PLAYER2_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], DBUS_TYPE_MEDIA_PLAYER2);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, DBUS_TYPE_MEDIA_PLAYER2);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_dbus_media_player2_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  DbusMediaPlayer2Skeleton *skeleton = DBUS_MEDIA_PLAYER2_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_media_player2_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_dbus_media_player2_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  DbusMediaPlayer2Skeleton *skeleton = DBUS_MEDIA_PLAYER2_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_media_player2_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _dbus_media_player2_skeleton_vtable =
{
  _dbus_media_player2_skeleton_handle_method_call,
  _dbus_media_player2_skeleton_handle_get_property,
  _dbus_media_player2_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
dbus_media_player2_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return dbus_media_player2_interface_info ();
}

static GDBusInterfaceVTable *
dbus_media_player2_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_dbus_media_player2_skeleton_vtable;
}

static GVariant *
dbus_media_player2_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  DbusMediaPlayer2Skeleton *skeleton = DBUS_MEDIA_PLAYER2_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_dbus_media_player2_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _dbus_media_player2_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _dbus_media_player2_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _dbus_media_player2_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.mpris.MediaPlayer2", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _dbus_media_player2_emit_changed (gpointer user_data);

static void
dbus_media_player2_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  DbusMediaPlayer2Skeleton *skeleton = DBUS_MEDIA_PLAYER2_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _dbus_media_player2_emit_changed (skeleton);
}

static void dbus_media_player2_skeleton_iface_init (DbusMediaPlayer2Iface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (DbusMediaPlayer2Skeleton, dbus_media_player2_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (DbusMediaPlayer2Skeleton)
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_MEDIA_PLAYER2, dbus_media_player2_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (DbusMediaPlayer2Skeleton, dbus_media_player2_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_MEDIA_PLAYER2, dbus_media_player2_skeleton_iface_init))

#endif
static void
dbus_media_player2_skeleton_finalize (GObject *object)
{
  DbusMediaPlayer2Skeleton *skeleton = DBUS_MEDIA_PLAYER2_SKELETON (object);
  guint n;
  for (n = 0; n < 8; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (dbus_media_player2_skeleton_parent_class)->finalize (object);
}

static void
dbus_media_player2_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  DbusMediaPlayer2Skeleton *skeleton = DBUS_MEDIA_PLAYER2_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 8);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_dbus_media_player2_emit_changed (gpointer user_data)
{
  DbusMediaPlayer2Skeleton *skeleton = DBUS_MEDIA_PLAYER2_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.mpris.MediaPlayer2",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_dbus_media_player2_schedule_emit_changed (DbusMediaPlayer2Skeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
dbus_media_player2_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  DbusMediaPlayer2Skeleton *skeleton = DBUS_MEDIA_PLAYER2_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _dbus_media_player2_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _dbus_media_player2_emit_changed");
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
dbus_media_player2_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  DbusMediaPlayer2Skeleton *skeleton = DBUS_MEDIA_PLAYER2_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 8);
  info = (const _ExtendedGDBusPropertyInfo *) _dbus_media_player2_property_info_pointers[prop_id - 1];
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL &&
          info->emits_changed_signal)
        _dbus_media_player2_schedule_emit_changed (skeleton, info, prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
dbus_media_player2_skeleton_init (DbusMediaPlayer2Skeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = dbus_media_player2_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, DBUS_TYPE_MEDIA_PLAYER2_SKELETON, DbusMediaPlayer2SkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 8);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[3], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[4], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[5], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[6], G_TYPE_STRV);
  g_value_init (&skeleton->priv->properties[7], G_TYPE_STRV);
}

static gboolean 
dbus_media_player2_skeleton_get_can_quit (DbusMediaPlayer2 *object)
{
  DbusMediaPlayer2Skeleton *skeleton = DBUS_MEDIA_PLAYER2_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_boolean (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
dbus_media_player2_skeleton_get_can_set_fullscreen (DbusMediaPlayer2 *object)
{
  DbusMediaPlayer2Skeleton *skeleton = DBUS_MEDIA_PLAYER2_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_boolean (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
dbus_media_player2_skeleton_get_can_raise (DbusMediaPlayer2 *object)
{
  DbusMediaPlayer2Skeleton *skeleton = DBUS_MEDIA_PLAYER2_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_boolean (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
dbus_media_player2_skeleton_get_has_track_list (DbusMediaPlayer2 *object)
{
  DbusMediaPlayer2Skeleton *skeleton = DBUS_MEDIA_PLAYER2_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_boolean (&(skeleton->priv->properties[3]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
dbus_media_player2_skeleton_get_identity (DbusMediaPlayer2 *object)
{
  DbusMediaPlayer2Skeleton *skeleton = DBUS_MEDIA_PLAYER2_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_string (&(skeleton->priv->properties[4]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
dbus_media_player2_skeleton_get_desktop_entry (DbusMediaPlayer2 *object)
{
  DbusMediaPlayer2Skeleton *skeleton = DBUS_MEDIA_PLAYER2_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_string (&(skeleton->priv->properties[5]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *const *
dbus_media_player2_skeleton_get_supported_uri_schemes (DbusMediaPlayer2 *object)
{
  DbusMediaPlayer2Skeleton *skeleton = DBUS_MEDIA_PLAYER2_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_boxed (&(skeleton->priv->properties[6]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *const *
dbus_media_player2_skeleton_get_supported_mime_types (DbusMediaPlayer2 *object)
{
  DbusMediaPlayer2Skeleton *skeleton = DBUS_MEDIA_PLAYER2_SKELETON (object);
  const gchar *const *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_boxed (&(skeleton->priv->properties[7]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
dbus_media_player2_skeleton_class_init (DbusMediaPlayer2SkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = dbus_media_player2_skeleton_finalize;
  gobject_class->get_property = dbus_media_player2_skeleton_get_property;
  gobject_class->set_property = dbus_media_player2_skeleton_set_property;
  gobject_class->notify       = dbus_media_player2_skeleton_notify;


  dbus_media_player2_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = dbus_media_player2_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = dbus_media_player2_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = dbus_media_player2_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = dbus_media_player2_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (DbusMediaPlayer2SkeletonPrivate));
#endif
}

static void
dbus_media_player2_skeleton_iface_init (DbusMediaPlayer2Iface *iface)
{
  iface->get_can_quit = dbus_media_player2_skeleton_get_can_quit;
  iface->get_can_set_fullscreen = dbus_media_player2_skeleton_get_can_set_fullscreen;
  iface->get_can_raise = dbus_media_player2_skeleton_get_can_raise;
  iface->get_has_track_list = dbus_media_player2_skeleton_get_has_track_list;
  iface->get_identity = dbus_media_player2_skeleton_get_identity;
  iface->get_desktop_entry = dbus_media_player2_skeleton_get_desktop_entry;
  iface->get_supported_uri_schemes = dbus_media_player2_skeleton_get_supported_uri_schemes;
  iface->get_supported_mime_types = dbus_media_player2_skeleton_get_supported_mime_types;
}

/**
 * dbus_media_player2_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2.top_of_page">org.mpris.MediaPlayer2</link>.
 *
 * Returns: (transfer full) (type DbusMediaPlayer2Skeleton): The skeleton object.
 */
DbusMediaPlayer2 *
dbus_media_player2_skeleton_new (void)
{
  return DBUS_MEDIA_PLAYER2 (g_object_new (DBUS_TYPE_MEDIA_PLAYER2_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.mpris.MediaPlayer2.Player
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:DbusMediaPlayer2Player
 * @title: DbusMediaPlayer2Player
 * @short_description: Generated C code for the org.mpris.MediaPlayer2.Player D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-mpris-MediaPlayer2-Player.top_of_page">org.mpris.MediaPlayer2.Player</link> D-Bus interface in C.
 */

enum
{
  DBUS__MEDIA_PLAYER2_PLAYER_SEEKED,
};

static unsigned DBUS__MEDIA_PLAYER2_PLAYER_SIGNALS[1] = { 0 };

/* ---- Introspection data for org.mpris.MediaPlayer2.Player ---- */

static const _ExtendedGDBusMethodInfo _dbus_media_player2_player_method_info_next =
{
  {
    -1,
    (gchar *) "Next",
    NULL,
    NULL,
    NULL
  },
  "handle-next",
  FALSE
};

static const _ExtendedGDBusMethodInfo _dbus_media_player2_player_method_info_previous =
{
  {
    -1,
    (gchar *) "Previous",
    NULL,
    NULL,
    NULL
  },
  "handle-previous",
  FALSE
};

static const _ExtendedGDBusMethodInfo _dbus_media_player2_player_method_info_pause =
{
  {
    -1,
    (gchar *) "Pause",
    NULL,
    NULL,
    NULL
  },
  "handle-pause",
  FALSE
};

static const _ExtendedGDBusMethodInfo _dbus_media_player2_player_method_info_play_pause =
{
  {
    -1,
    (gchar *) "PlayPause",
    NULL,
    NULL,
    NULL
  },
  "handle-play-pause",
  FALSE
};

static const _ExtendedGDBusMethodInfo _dbus_media_player2_player_method_info_stop =
{
  {
    -1,
    (gchar *) "Stop",
    NULL,
    NULL,
    NULL
  },
  "handle-stop",
  FALSE
};

static const _ExtendedGDBusMethodInfo _dbus_media_player2_player_method_info_play =
{
  {
    -1,
    (gchar *) "Play",
    NULL,
    NULL,
    NULL
  },
  "handle-play",
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_media_player2_player_method_info_seek_IN_ARG_Offset =
{
  {
    -1,
    (gchar *) "Offset",
    (gchar *) "x",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_media_player2_player_method_info_seek_IN_ARG_pointers[] =
{
  &_dbus_media_player2_player_method_info_seek_IN_ARG_Offset.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _dbus_media_player2_player_method_info_seek =
{
  {
    -1,
    (gchar *) "Seek",
    (GDBusArgInfo **) &_dbus_media_player2_player_method_info_seek_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-seek",
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_media_player2_player_method_info_set_position_IN_ARG_TrackId =
{
  {
    -1,
    (gchar *) "TrackId",
    (gchar *) "o",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_media_player2_player_method_info_set_position_IN_ARG_Position =
{
  {
    -1,
    (gchar *) "Position",
    (gchar *) "x",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_media_player2_player_method_info_set_position_IN_ARG_pointers[] =
{
  &_dbus_media_player2_player_method_info_set_position_IN_ARG_TrackId.parent_struct,
  &_dbus_media_player2_player_method_info_set_position_IN_ARG_Position.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _dbus_media_player2_player_method_info_set_position =
{
  {
    -1,
    (gchar *) "SetPosition",
    (GDBusArgInfo **) &_dbus_media_player2_player_method_info_set_position_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-set-position",
  FALSE
};

static const _ExtendedGDBusArgInfo _dbus_media_player2_player_method_info_open_uri_IN_ARG_Uri =
{
  {
    -1,
    (gchar *) "Uri",
    (gchar *) "s",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_media_player2_player_method_info_open_uri_IN_ARG_pointers[] =
{
  &_dbus_media_player2_player_method_info_open_uri_IN_ARG_Uri.parent_struct,
  NULL
};

static const _ExtendedGDBusMethodInfo _dbus_media_player2_player_method_info_open_uri =
{
  {
    -1,
    (gchar *) "OpenUri",
    (GDBusArgInfo **) &_dbus_media_player2_player_method_info_open_uri_IN_ARG_pointers,
    NULL,
    NULL
  },
  "handle-open-uri",
  FALSE
};

static const GDBusMethodInfo * const _dbus_media_player2_player_method_info_pointers[] =
{
  &_dbus_media_player2_player_method_info_next.parent_struct,
  &_dbus_media_player2_player_method_info_previous.parent_struct,
  &_dbus_media_player2_player_method_info_pause.parent_struct,
  &_dbus_media_player2_player_method_info_play_pause.parent_struct,
  &_dbus_media_player2_player_method_info_stop.parent_struct,
  &_dbus_media_player2_player_method_info_play.parent_struct,
  &_dbus_media_player2_player_method_info_seek.parent_struct,
  &_dbus_media_player2_player_method_info_set_position.parent_struct,
  &_dbus_media_player2_player_method_info_open_uri.parent_struct,
  NULL
};

static const _ExtendedGDBusArgInfo _dbus_media_player2_player_signal_info_seeked_ARG_Position =
{
  {
    -1,
    (gchar *) "Position",
    (gchar *) "x",
    NULL
  },
  FALSE
};

static const GDBusArgInfo * const _dbus_media_player2_player_signal_info_seeked_ARG_pointers[] =
{
  &_dbus_media_player2_player_signal_info_seeked_ARG_Position.parent_struct,
  NULL
};

static const _ExtendedGDBusSignalInfo _dbus_media_player2_player_signal_info_seeked =
{
  {
    -1,
    (gchar *) "Seeked",
    (GDBusArgInfo **) &_dbus_media_player2_player_signal_info_seeked_ARG_pointers,
    NULL
  },
  "seeked"
};

static const GDBusSignalInfo * const _dbus_media_player2_player_signal_info_pointers[] =
{
  &_dbus_media_player2_player_signal_info_seeked.parent_struct,
  NULL
};

static const _ExtendedGDBusPropertyInfo _dbus_media_player2_player_property_info_playback_status =
{
  {
    -1,
    (gchar *) "PlaybackStatus",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "playback-status",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _dbus_media_player2_player_property_info_loop_status =
{
  {
    -1,
    (gchar *) "LoopStatus",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "loop-status",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _dbus_media_player2_player_property_info_rate =
{
  {
    -1,
    (gchar *) "Rate",
    (gchar *) "d",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "rate",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _dbus_media_player2_player_property_info_shuffle =
{
  {
    -1,
    (gchar *) "Shuffle",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "shuffle",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _dbus_media_player2_player_property_info_metadata =
{
  {
    -1,
    (gchar *) "Metadata",
    (gchar *) "a{sv}",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "metadata",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _dbus_media_player2_player_property_info_volume =
{
  {
    -1,
    (gchar *) "Volume",
    (gchar *) "d",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE | G_DBUS_PROPERTY_INFO_FLAGS_WRITABLE,
    NULL
  },
  "volume",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _dbus_media_player2_player_property_info_position =
{
  {
    -1,
    (gchar *) "Position",
    (gchar *) "x",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "position",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _dbus_media_player2_player_property_info_minimum_rate =
{
  {
    -1,
    (gchar *) "MinimumRate",
    (gchar *) "d",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "minimum-rate",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _dbus_media_player2_player_property_info_maximum_rate =
{
  {
    -1,
    (gchar *) "MaximumRate",
    (gchar *) "d",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "maximum-rate",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _dbus_media_player2_player_property_info_can_go_next =
{
  {
    -1,
    (gchar *) "CanGoNext",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "can-go-next",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _dbus_media_player2_player_property_info_can_go_previous =
{
  {
    -1,
    (gchar *) "CanGoPrevious",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "can-go-previous",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _dbus_media_player2_player_property_info_can_play =
{
  {
    -1,
    (gchar *) "CanPlay",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "can-play",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _dbus_media_player2_player_property_info_can_pause =
{
  {
    -1,
    (gchar *) "CanPause",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "can-pause",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _dbus_media_player2_player_property_info_can_seek =
{
  {
    -1,
    (gchar *) "CanSeek",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "can-seek",
  FALSE,
  TRUE
};

static const _ExtendedGDBusPropertyInfo _dbus_media_player2_player_property_info_can_control =
{
  {
    -1,
    (gchar *) "CanControl",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "can-control",
  FALSE,
  TRUE
};

static const GDBusPropertyInfo * const _dbus_media_player2_player_property_info_pointers[] =
{
  &_dbus_media_player2_player_property_info_playback_status.parent_struct,
  &_dbus_media_player2_player_property_info_loop_status.parent_struct,
  &_dbus_media_player2_player_property_info_rate.parent_struct,
  &_dbus_media_player2_player_property_info_shuffle.parent_struct,
  &_dbus_media_player2_player_property_info_metadata.parent_struct,
  &_dbus_media_player2_player_property_info_volume.parent_struct,
  &_dbus_media_player2_player_property_info_position.parent_struct,
  &_dbus_media_player2_player_property_info_minimum_rate.parent_struct,
  &_dbus_media_player2_player_property_info_maximum_rate.parent_struct,
  &_dbus_media_player2_player_property_info_can_go_next.parent_struct,
  &_dbus_media_player2_player_property_info_can_go_previous.parent_struct,
  &_dbus_media_player2_player_property_info_can_play.parent_struct,
  &_dbus_media_player2_player_property_info_can_pause.parent_struct,
  &_dbus_media_player2_player_property_info_can_seek.parent_struct,
  &_dbus_media_player2_player_property_info_can_control.parent_struct,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _dbus_media_player2_player_interface_info =
{
  {
    -1,
    (gchar *) "org.mpris.MediaPlayer2.Player",
    (GDBusMethodInfo **) &_dbus_media_player2_player_method_info_pointers,
    (GDBusSignalInfo **) &_dbus_media_player2_player_signal_info_pointers,
    (GDBusPropertyInfo **) &_dbus_media_player2_player_property_info_pointers,
    NULL
  },
  "media-player2-player",
};


/**
 * dbus_media_player2_player_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-mpris-MediaPlayer2-Player.top_of_page">org.mpris.MediaPlayer2.Player</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
dbus_media_player2_player_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_dbus_media_player2_player_interface_info.parent_struct;
}

/**
 * dbus_media_player2_player_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #DbusMediaPlayer2Player interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
dbus_media_player2_player_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "playback-status");
  g_object_class_override_property (klass, property_id_begin++, "loop-status");
  g_object_class_override_property (klass, property_id_begin++, "rate");
  g_object_class_override_property (klass, property_id_begin++, "shuffle");
  g_object_class_override_property (klass, property_id_begin++, "metadata");
  g_object_class_override_property (klass, property_id_begin++, "volume");
  g_object_class_override_property (klass, property_id_begin++, "position");
  g_object_class_override_property (klass, property_id_begin++, "minimum-rate");
  g_object_class_override_property (klass, property_id_begin++, "maximum-rate");
  g_object_class_override_property (klass, property_id_begin++, "can-go-next");
  g_object_class_override_property (klass, property_id_begin++, "can-go-previous");
  g_object_class_override_property (klass, property_id_begin++, "can-play");
  g_object_class_override_property (klass, property_id_begin++, "can-pause");
  g_object_class_override_property (klass, property_id_begin++, "can-seek");
  g_object_class_override_property (klass, property_id_begin++, "can-control");
  return property_id_begin - 1;
}


inline static void
dbus_media_player2_player_signal_marshal_seeked (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_VOID__INT64 (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_media_player2_player_method_marshal_next (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_media_player2_player_method_marshal_previous (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_media_player2_player_method_marshal_pause (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_media_player2_player_method_marshal_play_pause (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_media_player2_player_method_marshal_stop (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_media_player2_player_method_marshal_play (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_media_player2_player_method_marshal_seek (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT_INT64 (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_media_player2_player_method_marshal_set_position (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT_STRING_INT64 (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}

inline static void
dbus_media_player2_player_method_marshal_open_uri (
    GClosure     *closure,
    GValue       *return_value,
    unsigned int  n_param_values,
    const GValue *param_values,
    void         *invocation_hint,
    void         *marshal_data)
{
  _g_dbus_codegen_marshal_BOOLEAN__OBJECT_STRING (closure,
    return_value, n_param_values, param_values, invocation_hint, marshal_data);
}


/**
 * DbusMediaPlayer2Player:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2-Player.top_of_page">org.mpris.MediaPlayer2.Player</link>.
 */

/**
 * DbusMediaPlayer2PlayerIface:
 * @parent_iface: The parent interface.
 * @handle_next: Handler for the #DbusMediaPlayer2Player::handle-next signal.
 * @handle_open_uri: Handler for the #DbusMediaPlayer2Player::handle-open-uri signal.
 * @handle_pause: Handler for the #DbusMediaPlayer2Player::handle-pause signal.
 * @handle_play: Handler for the #DbusMediaPlayer2Player::handle-play signal.
 * @handle_play_pause: Handler for the #DbusMediaPlayer2Player::handle-play-pause signal.
 * @handle_previous: Handler for the #DbusMediaPlayer2Player::handle-previous signal.
 * @handle_seek: Handler for the #DbusMediaPlayer2Player::handle-seek signal.
 * @handle_set_position: Handler for the #DbusMediaPlayer2Player::handle-set-position signal.
 * @handle_stop: Handler for the #DbusMediaPlayer2Player::handle-stop signal.
 * @get_can_control: Getter for the #DbusMediaPlayer2Player:can-control property.
 * @get_can_go_next: Getter for the #DbusMediaPlayer2Player:can-go-next property.
 * @get_can_go_previous: Getter for the #DbusMediaPlayer2Player:can-go-previous property.
 * @get_can_pause: Getter for the #DbusMediaPlayer2Player:can-pause property.
 * @get_can_play: Getter for the #DbusMediaPlayer2Player:can-play property.
 * @get_can_seek: Getter for the #DbusMediaPlayer2Player:can-seek property.
 * @get_loop_status: Getter for the #DbusMediaPlayer2Player:loop-status property.
 * @get_maximum_rate: Getter for the #DbusMediaPlayer2Player:maximum-rate property.
 * @get_metadata: Getter for the #DbusMediaPlayer2Player:metadata property.
 * @get_minimum_rate: Getter for the #DbusMediaPlayer2Player:minimum-rate property.
 * @get_playback_status: Getter for the #DbusMediaPlayer2Player:playback-status property.
 * @get_position: Getter for the #DbusMediaPlayer2Player:position property.
 * @get_rate: Getter for the #DbusMediaPlayer2Player:rate property.
 * @get_shuffle: Getter for the #DbusMediaPlayer2Player:shuffle property.
 * @get_volume: Getter for the #DbusMediaPlayer2Player:volume property.
 * @seeked: Handler for the #DbusMediaPlayer2Player::seeked signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2-Player.top_of_page">org.mpris.MediaPlayer2.Player</link>.
 */

typedef DbusMediaPlayer2PlayerIface DbusMediaPlayer2PlayerInterface;
G_DEFINE_INTERFACE (DbusMediaPlayer2Player, dbus_media_player2_player, G_TYPE_OBJECT)

static void
dbus_media_player2_player_default_init (DbusMediaPlayer2PlayerIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * DbusMediaPlayer2Player::handle-next:
   * @object: A #DbusMediaPlayer2Player.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Next">Next()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_media_player2_player_complete_next() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-next",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusMediaPlayer2PlayerIface, handle_next),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_media_player2_player_method_marshal_next,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * DbusMediaPlayer2Player::handle-previous:
   * @object: A #DbusMediaPlayer2Player.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Previous">Previous()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_media_player2_player_complete_previous() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-previous",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusMediaPlayer2PlayerIface, handle_previous),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_media_player2_player_method_marshal_previous,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * DbusMediaPlayer2Player::handle-pause:
   * @object: A #DbusMediaPlayer2Player.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Pause">Pause()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_media_player2_player_complete_pause() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-pause",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusMediaPlayer2PlayerIface, handle_pause),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_media_player2_player_method_marshal_pause,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * DbusMediaPlayer2Player::handle-play-pause:
   * @object: A #DbusMediaPlayer2Player.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.PlayPause">PlayPause()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_media_player2_player_complete_play_pause() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-play-pause",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusMediaPlayer2PlayerIface, handle_play_pause),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_media_player2_player_method_marshal_play_pause,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * DbusMediaPlayer2Player::handle-stop:
   * @object: A #DbusMediaPlayer2Player.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Stop">Stop()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_media_player2_player_complete_stop() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-stop",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusMediaPlayer2PlayerIface, handle_stop),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_media_player2_player_method_marshal_stop,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * DbusMediaPlayer2Player::handle-play:
   * @object: A #DbusMediaPlayer2Player.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Play">Play()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_media_player2_player_complete_play() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-play",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusMediaPlayer2PlayerIface, handle_play),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_media_player2_player_method_marshal_play,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * DbusMediaPlayer2Player::handle-seek:
   * @object: A #DbusMediaPlayer2Player.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_Offset: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Seek">Seek()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_media_player2_player_complete_seek() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-seek",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusMediaPlayer2PlayerIface, handle_seek),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_media_player2_player_method_marshal_seek,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_INT64);

  /**
   * DbusMediaPlayer2Player::handle-set-position:
   * @object: A #DbusMediaPlayer2Player.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_TrackId: Argument passed by remote caller.
   * @arg_Position: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.SetPosition">SetPosition()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_media_player2_player_complete_set_position() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-set-position",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusMediaPlayer2PlayerIface, handle_set_position),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_media_player2_player_method_marshal_set_position,
    G_TYPE_BOOLEAN,
    3,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING, G_TYPE_INT64);

  /**
   * DbusMediaPlayer2Player::handle-open-uri:
   * @object: A #DbusMediaPlayer2Player.
   * @invocation: A #GDBusMethodInvocation.
   * @arg_Uri: Argument passed by remote caller.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.OpenUri">OpenUri()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call dbus_media_player2_player_complete_open_uri() or e.g. g_dbus_method_invocation_return_error() on it) and no other signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %G_DBUS_METHOD_INVOCATION_HANDLED or %TRUE if the invocation was handled, %G_DBUS_METHOD_INVOCATION_UNHANDLED or %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-open-uri",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (DbusMediaPlayer2PlayerIface, handle_open_uri),
    g_signal_accumulator_true_handled,
    NULL,
      dbus_media_player2_player_method_marshal_open_uri,
    G_TYPE_BOOLEAN,
    2,
    G_TYPE_DBUS_METHOD_INVOCATION, G_TYPE_STRING);

  /* GObject signals for received D-Bus signals: */
  /**
   * DbusMediaPlayer2Player::seeked:
   * @object: A #DbusMediaPlayer2Player.
   * @arg_Position: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-mpris-MediaPlayer2-Player.Seeked">"Seeked"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  DBUS__MEDIA_PLAYER2_PLAYER_SIGNALS[DBUS__MEDIA_PLAYER2_PLAYER_SEEKED] =
    g_signal_new ("seeked",
      G_TYPE_FROM_INTERFACE (iface),
      G_SIGNAL_RUN_LAST,
      G_STRUCT_OFFSET (DbusMediaPlayer2PlayerIface, seeked),
      NULL,
      NULL,
      dbus_media_player2_player_signal_marshal_seeked,
      G_TYPE_NONE,
      1, G_TYPE_INT64);

  /* GObject properties for D-Bus properties: */
  /**
   * DbusMediaPlayer2Player:playback-status:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.PlaybackStatus">"PlaybackStatus"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("playback-status", "PlaybackStatus", "PlaybackStatus", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * DbusMediaPlayer2Player:loop-status:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.LoopStatus">"LoopStatus"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("loop-status", "LoopStatus", "LoopStatus", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * DbusMediaPlayer2Player:rate:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Rate">"Rate"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_double ("rate", "Rate", "Rate", -G_MAXDOUBLE, G_MAXDOUBLE, 0.0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * DbusMediaPlayer2Player:shuffle:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Shuffle">"Shuffle"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("shuffle", "Shuffle", "Shuffle", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * DbusMediaPlayer2Player:metadata:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Metadata">"Metadata"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_variant ("metadata", "Metadata", "Metadata", G_VARIANT_TYPE ("a{sv}"), NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * DbusMediaPlayer2Player:volume:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Volume">"Volume"</link>.
   *
   * Since the D-Bus property for this #GObject property is both readable and writable, it is meaningful to both read from it and write to it on both the service- and client-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_double ("volume", "Volume", "Volume", -G_MAXDOUBLE, G_MAXDOUBLE, 0.0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * DbusMediaPlayer2Player:position:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Position">"Position"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_int64 ("position", "Position", "Position", G_MININT64, G_MAXINT64, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * DbusMediaPlayer2Player:minimum-rate:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.MinimumRate">"MinimumRate"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_double ("minimum-rate", "MinimumRate", "MinimumRate", -G_MAXDOUBLE, G_MAXDOUBLE, 0.0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * DbusMediaPlayer2Player:maximum-rate:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.MaximumRate">"MaximumRate"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_double ("maximum-rate", "MaximumRate", "MaximumRate", -G_MAXDOUBLE, G_MAXDOUBLE, 0.0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * DbusMediaPlayer2Player:can-go-next:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanGoNext">"CanGoNext"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("can-go-next", "CanGoNext", "CanGoNext", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * DbusMediaPlayer2Player:can-go-previous:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanGoPrevious">"CanGoPrevious"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("can-go-previous", "CanGoPrevious", "CanGoPrevious", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * DbusMediaPlayer2Player:can-play:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanPlay">"CanPlay"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("can-play", "CanPlay", "CanPlay", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * DbusMediaPlayer2Player:can-pause:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanPause">"CanPause"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("can-pause", "CanPause", "CanPause", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * DbusMediaPlayer2Player:can-seek:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanSeek">"CanSeek"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("can-seek", "CanSeek", "CanSeek", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * DbusMediaPlayer2Player:can-control:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanControl">"CanControl"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("can-control", "CanControl", "CanControl", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * dbus_media_player2_player_get_playback_status: (skip)
 * @object: A #DbusMediaPlayer2Player.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.PlaybackStatus">"PlaybackStatus"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use dbus_media_player2_player_dup_playback_status() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
dbus_media_player2_player_get_playback_status (DbusMediaPlayer2Player *object)
{
  g_return_val_if_fail (DBUS_IS_MEDIA_PLAYER2_PLAYER (object), NULL);

  return DBUS_MEDIA_PLAYER2_PLAYER_GET_IFACE (object)->get_playback_status (object);
}

/**
 * dbus_media_player2_player_dup_playback_status: (skip)
 * @object: A #DbusMediaPlayer2Player.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.PlaybackStatus">"PlaybackStatus"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
dbus_media_player2_player_dup_playback_status (DbusMediaPlayer2Player *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "playback-status", &value, NULL);
  return value;
}

/**
 * dbus_media_player2_player_set_playback_status: (skip)
 * @object: A #DbusMediaPlayer2Player.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.PlaybackStatus">"PlaybackStatus"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
dbus_media_player2_player_set_playback_status (DbusMediaPlayer2Player *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "playback-status", value, NULL);
}

/**
 * dbus_media_player2_player_get_loop_status: (skip)
 * @object: A #DbusMediaPlayer2Player.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.LoopStatus">"LoopStatus"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use dbus_media_player2_player_dup_loop_status() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
dbus_media_player2_player_get_loop_status (DbusMediaPlayer2Player *object)
{
  g_return_val_if_fail (DBUS_IS_MEDIA_PLAYER2_PLAYER (object), NULL);

  return DBUS_MEDIA_PLAYER2_PLAYER_GET_IFACE (object)->get_loop_status (object);
}

/**
 * dbus_media_player2_player_dup_loop_status: (skip)
 * @object: A #DbusMediaPlayer2Player.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.LoopStatus">"LoopStatus"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
dbus_media_player2_player_dup_loop_status (DbusMediaPlayer2Player *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "loop-status", &value, NULL);
  return value;
}

/**
 * dbus_media_player2_player_set_loop_status: (skip)
 * @object: A #DbusMediaPlayer2Player.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.LoopStatus">"LoopStatus"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
dbus_media_player2_player_set_loop_status (DbusMediaPlayer2Player *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "loop-status", value, NULL);
}

/**
 * dbus_media_player2_player_get_rate: (skip)
 * @object: A #DbusMediaPlayer2Player.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Rate">"Rate"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gdouble 
dbus_media_player2_player_get_rate (DbusMediaPlayer2Player *object)
{
  g_return_val_if_fail (DBUS_IS_MEDIA_PLAYER2_PLAYER (object), 0.0);

  return DBUS_MEDIA_PLAYER2_PLAYER_GET_IFACE (object)->get_rate (object);
}

/**
 * dbus_media_player2_player_set_rate: (skip)
 * @object: A #DbusMediaPlayer2Player.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Rate">"Rate"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
dbus_media_player2_player_set_rate (DbusMediaPlayer2Player *object, gdouble value)
{
  g_object_set (G_OBJECT (object), "rate", value, NULL);
}

/**
 * dbus_media_player2_player_get_shuffle: (skip)
 * @object: A #DbusMediaPlayer2Player.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Shuffle">"Shuffle"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
dbus_media_player2_player_get_shuffle (DbusMediaPlayer2Player *object)
{
  g_return_val_if_fail (DBUS_IS_MEDIA_PLAYER2_PLAYER (object), FALSE);

  return DBUS_MEDIA_PLAYER2_PLAYER_GET_IFACE (object)->get_shuffle (object);
}

/**
 * dbus_media_player2_player_set_shuffle: (skip)
 * @object: A #DbusMediaPlayer2Player.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Shuffle">"Shuffle"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
dbus_media_player2_player_set_shuffle (DbusMediaPlayer2Player *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "shuffle", value, NULL);
}

/**
 * dbus_media_player2_player_get_metadata: (skip)
 * @object: A #DbusMediaPlayer2Player.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Metadata">"Metadata"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use dbus_media_player2_player_dup_metadata() if on another thread.
 *
 * Returns: (transfer none) (nullable): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
GVariant *
dbus_media_player2_player_get_metadata (DbusMediaPlayer2Player *object)
{
  g_return_val_if_fail (DBUS_IS_MEDIA_PLAYER2_PLAYER (object), NULL);

  return DBUS_MEDIA_PLAYER2_PLAYER_GET_IFACE (object)->get_metadata (object);
}

/**
 * dbus_media_player2_player_dup_metadata: (skip)
 * @object: A #DbusMediaPlayer2Player.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Metadata">"Metadata"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full) (nullable): The property value or %NULL if the property is not set. The returned value should be freed with g_variant_unref().
 */
GVariant *
dbus_media_player2_player_dup_metadata (DbusMediaPlayer2Player *object)
{
  GVariant *value;
  g_object_get (G_OBJECT (object), "metadata", &value, NULL);
  return value;
}

/**
 * dbus_media_player2_player_set_metadata: (skip)
 * @object: A #DbusMediaPlayer2Player.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Metadata">"Metadata"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
dbus_media_player2_player_set_metadata (DbusMediaPlayer2Player *object, GVariant *value)
{
  g_object_set (G_OBJECT (object), "metadata", value, NULL);
}

/**
 * dbus_media_player2_player_get_volume: (skip)
 * @object: A #DbusMediaPlayer2Player.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Volume">"Volume"</link> D-Bus property.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gdouble 
dbus_media_player2_player_get_volume (DbusMediaPlayer2Player *object)
{
  g_return_val_if_fail (DBUS_IS_MEDIA_PLAYER2_PLAYER (object), 0.0);

  return DBUS_MEDIA_PLAYER2_PLAYER_GET_IFACE (object)->get_volume (object);
}

/**
 * dbus_media_player2_player_set_volume: (skip)
 * @object: A #DbusMediaPlayer2Player.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Volume">"Volume"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is both readable and writable, it is meaningful to use this function on both the client- and service-side.
 */
void
dbus_media_player2_player_set_volume (DbusMediaPlayer2Player *object, gdouble value)
{
  g_object_set (G_OBJECT (object), "volume", value, NULL);
}

/**
 * dbus_media_player2_player_get_position: (skip)
 * @object: A #DbusMediaPlayer2Player.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Position">"Position"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gint64 
dbus_media_player2_player_get_position (DbusMediaPlayer2Player *object)
{
  g_return_val_if_fail (DBUS_IS_MEDIA_PLAYER2_PLAYER (object), 0);

  return DBUS_MEDIA_PLAYER2_PLAYER_GET_IFACE (object)->get_position (object);
}

/**
 * dbus_media_player2_player_set_position: (skip)
 * @object: A #DbusMediaPlayer2Player.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.Position">"Position"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
dbus_media_player2_player_set_position (DbusMediaPlayer2Player *object, gint64 value)
{
  g_object_set (G_OBJECT (object), "position", value, NULL);
}

/**
 * dbus_media_player2_player_get_minimum_rate: (skip)
 * @object: A #DbusMediaPlayer2Player.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.MinimumRate">"MinimumRate"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gdouble 
dbus_media_player2_player_get_minimum_rate (DbusMediaPlayer2Player *object)
{
  g_return_val_if_fail (DBUS_IS_MEDIA_PLAYER2_PLAYER (object), 0.0);

  return DBUS_MEDIA_PLAYER2_PLAYER_GET_IFACE (object)->get_minimum_rate (object);
}

/**
 * dbus_media_player2_player_set_minimum_rate: (skip)
 * @object: A #DbusMediaPlayer2Player.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.MinimumRate">"MinimumRate"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
dbus_media_player2_player_set_minimum_rate (DbusMediaPlayer2Player *object, gdouble value)
{
  g_object_set (G_OBJECT (object), "minimum-rate", value, NULL);
}

/**
 * dbus_media_player2_player_get_maximum_rate: (skip)
 * @object: A #DbusMediaPlayer2Player.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.MaximumRate">"MaximumRate"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gdouble 
dbus_media_player2_player_get_maximum_rate (DbusMediaPlayer2Player *object)
{
  g_return_val_if_fail (DBUS_IS_MEDIA_PLAYER2_PLAYER (object), 0.0);

  return DBUS_MEDIA_PLAYER2_PLAYER_GET_IFACE (object)->get_maximum_rate (object);
}

/**
 * dbus_media_player2_player_set_maximum_rate: (skip)
 * @object: A #DbusMediaPlayer2Player.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.MaximumRate">"MaximumRate"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
dbus_media_player2_player_set_maximum_rate (DbusMediaPlayer2Player *object, gdouble value)
{
  g_object_set (G_OBJECT (object), "maximum-rate", value, NULL);
}

/**
 * dbus_media_player2_player_get_can_go_next: (skip)
 * @object: A #DbusMediaPlayer2Player.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanGoNext">"CanGoNext"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
dbus_media_player2_player_get_can_go_next (DbusMediaPlayer2Player *object)
{
  g_return_val_if_fail (DBUS_IS_MEDIA_PLAYER2_PLAYER (object), FALSE);

  return DBUS_MEDIA_PLAYER2_PLAYER_GET_IFACE (object)->get_can_go_next (object);
}

/**
 * dbus_media_player2_player_set_can_go_next: (skip)
 * @object: A #DbusMediaPlayer2Player.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanGoNext">"CanGoNext"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
dbus_media_player2_player_set_can_go_next (DbusMediaPlayer2Player *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "can-go-next", value, NULL);
}

/**
 * dbus_media_player2_player_get_can_go_previous: (skip)
 * @object: A #DbusMediaPlayer2Player.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanGoPrevious">"CanGoPrevious"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
dbus_media_player2_player_get_can_go_previous (DbusMediaPlayer2Player *object)
{
  g_return_val_if_fail (DBUS_IS_MEDIA_PLAYER2_PLAYER (object), FALSE);

  return DBUS_MEDIA_PLAYER2_PLAYER_GET_IFACE (object)->get_can_go_previous (object);
}

/**
 * dbus_media_player2_player_set_can_go_previous: (skip)
 * @object: A #DbusMediaPlayer2Player.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanGoPrevious">"CanGoPrevious"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
dbus_media_player2_player_set_can_go_previous (DbusMediaPlayer2Player *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "can-go-previous", value, NULL);
}

/**
 * dbus_media_player2_player_get_can_play: (skip)
 * @object: A #DbusMediaPlayer2Player.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanPlay">"CanPlay"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
dbus_media_player2_player_get_can_play (DbusMediaPlayer2Player *object)
{
  g_return_val_if_fail (DBUS_IS_MEDIA_PLAYER2_PLAYER (object), FALSE);

  return DBUS_MEDIA_PLAYER2_PLAYER_GET_IFACE (object)->get_can_play (object);
}

/**
 * dbus_media_player2_player_set_can_play: (skip)
 * @object: A #DbusMediaPlayer2Player.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanPlay">"CanPlay"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
dbus_media_player2_player_set_can_play (DbusMediaPlayer2Player *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "can-play", value, NULL);
}

/**
 * dbus_media_player2_player_get_can_pause: (skip)
 * @object: A #DbusMediaPlayer2Player.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanPause">"CanPause"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
dbus_media_player2_player_get_can_pause (DbusMediaPlayer2Player *object)
{
  g_return_val_if_fail (DBUS_IS_MEDIA_PLAYER2_PLAYER (object), FALSE);

  return DBUS_MEDIA_PLAYER2_PLAYER_GET_IFACE (object)->get_can_pause (object);
}

/**
 * dbus_media_player2_player_set_can_pause: (skip)
 * @object: A #DbusMediaPlayer2Player.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanPause">"CanPause"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
dbus_media_player2_player_set_can_pause (DbusMediaPlayer2Player *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "can-pause", value, NULL);
}

/**
 * dbus_media_player2_player_get_can_seek: (skip)
 * @object: A #DbusMediaPlayer2Player.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanSeek">"CanSeek"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
dbus_media_player2_player_get_can_seek (DbusMediaPlayer2Player *object)
{
  g_return_val_if_fail (DBUS_IS_MEDIA_PLAYER2_PLAYER (object), FALSE);

  return DBUS_MEDIA_PLAYER2_PLAYER_GET_IFACE (object)->get_can_seek (object);
}

/**
 * dbus_media_player2_player_set_can_seek: (skip)
 * @object: A #DbusMediaPlayer2Player.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanSeek">"CanSeek"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
dbus_media_player2_player_set_can_seek (DbusMediaPlayer2Player *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "can-seek", value, NULL);
}

/**
 * dbus_media_player2_player_get_can_control: (skip)
 * @object: A #DbusMediaPlayer2Player.
 *
 * Gets the value of the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanControl">"CanControl"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
dbus_media_player2_player_get_can_control (DbusMediaPlayer2Player *object)
{
  g_return_val_if_fail (DBUS_IS_MEDIA_PLAYER2_PLAYER (object), FALSE);

  return DBUS_MEDIA_PLAYER2_PLAYER_GET_IFACE (object)->get_can_control (object);
}

/**
 * dbus_media_player2_player_set_can_control: (skip)
 * @object: A #DbusMediaPlayer2Player.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-mpris-MediaPlayer2-Player.CanControl">"CanControl"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
dbus_media_player2_player_set_can_control (DbusMediaPlayer2Player *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "can-control", value, NULL);
}

/**
 * dbus_media_player2_player_emit_seeked:
 * @object: A #DbusMediaPlayer2Player.
 * @arg_Position: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-mpris-MediaPlayer2-Player.Seeked">"Seeked"</link> D-Bus signal.
 */
void
dbus_media_player2_player_emit_seeked (
    DbusMediaPlayer2Player *object,
    gint64 arg_Position)
{
  g_signal_emit (object, DBUS__MEDIA_PLAYER2_PLAYER_SIGNALS[DBUS__MEDIA_PLAYER2_PLAYER_SEEKED], 0, arg_Position);
}

/**
 * dbus_media_player2_player_call_next:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Next">Next()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_media_player2_player_call_next_finish() to get the result of the operation.
 *
 * See dbus_media_player2_player_call_next_sync() for the synchronous, blocking version of this method.
 */
void
dbus_media_player2_player_call_next (
    DbusMediaPlayer2Player *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Next",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_media_player2_player_call_next_finish:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_media_player2_player_call_next().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_media_player2_player_call_next().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_media_player2_player_call_next_finish (
    DbusMediaPlayer2Player *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_media_player2_player_call_next_sync:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Next">Next()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_media_player2_player_call_next() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_media_player2_player_call_next_sync (
    DbusMediaPlayer2Player *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Next",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_media_player2_player_call_previous:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Previous">Previous()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_media_player2_player_call_previous_finish() to get the result of the operation.
 *
 * See dbus_media_player2_player_call_previous_sync() for the synchronous, blocking version of this method.
 */
void
dbus_media_player2_player_call_previous (
    DbusMediaPlayer2Player *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Previous",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_media_player2_player_call_previous_finish:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_media_player2_player_call_previous().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_media_player2_player_call_previous().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_media_player2_player_call_previous_finish (
    DbusMediaPlayer2Player *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_media_player2_player_call_previous_sync:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Previous">Previous()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_media_player2_player_call_previous() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_media_player2_player_call_previous_sync (
    DbusMediaPlayer2Player *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Previous",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_media_player2_player_call_pause:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Pause">Pause()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_media_player2_player_call_pause_finish() to get the result of the operation.
 *
 * See dbus_media_player2_player_call_pause_sync() for the synchronous, blocking version of this method.
 */
void
dbus_media_player2_player_call_pause (
    DbusMediaPlayer2Player *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Pause",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_media_player2_player_call_pause_finish:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_media_player2_player_call_pause().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_media_player2_player_call_pause().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_media_player2_player_call_pause_finish (
    DbusMediaPlayer2Player *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_media_player2_player_call_pause_sync:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Pause">Pause()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_media_player2_player_call_pause() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_media_player2_player_call_pause_sync (
    DbusMediaPlayer2Player *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Pause",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_media_player2_player_call_play_pause:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.PlayPause">PlayPause()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_media_player2_player_call_play_pause_finish() to get the result of the operation.
 *
 * See dbus_media_player2_player_call_play_pause_sync() for the synchronous, blocking version of this method.
 */
void
dbus_media_player2_player_call_play_pause (
    DbusMediaPlayer2Player *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "PlayPause",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_media_player2_player_call_play_pause_finish:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_media_player2_player_call_play_pause().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_media_player2_player_call_play_pause().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_media_player2_player_call_play_pause_finish (
    DbusMediaPlayer2Player *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_media_player2_player_call_play_pause_sync:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.PlayPause">PlayPause()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_media_player2_player_call_play_pause() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_media_player2_player_call_play_pause_sync (
    DbusMediaPlayer2Player *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "PlayPause",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_media_player2_player_call_stop:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Stop">Stop()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_media_player2_player_call_stop_finish() to get the result of the operation.
 *
 * See dbus_media_player2_player_call_stop_sync() for the synchronous, blocking version of this method.
 */
void
dbus_media_player2_player_call_stop (
    DbusMediaPlayer2Player *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Stop",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_media_player2_player_call_stop_finish:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_media_player2_player_call_stop().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_media_player2_player_call_stop().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_media_player2_player_call_stop_finish (
    DbusMediaPlayer2Player *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_media_player2_player_call_stop_sync:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Stop">Stop()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_media_player2_player_call_stop() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_media_player2_player_call_stop_sync (
    DbusMediaPlayer2Player *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Stop",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_media_player2_player_call_play:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Play">Play()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_media_player2_player_call_play_finish() to get the result of the operation.
 *
 * See dbus_media_player2_player_call_play_sync() for the synchronous, blocking version of this method.
 */
void
dbus_media_player2_player_call_play (
    DbusMediaPlayer2Player *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Play",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_media_player2_player_call_play_finish:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_media_player2_player_call_play().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_media_player2_player_call_play().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_media_player2_player_call_play_finish (
    DbusMediaPlayer2Player *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_media_player2_player_call_play_sync:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Play">Play()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_media_player2_player_call_play() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_media_player2_player_call_play_sync (
    DbusMediaPlayer2Player *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Play",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_media_player2_player_call_seek:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @arg_Offset: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Seek">Seek()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_media_player2_player_call_seek_finish() to get the result of the operation.
 *
 * See dbus_media_player2_player_call_seek_sync() for the synchronous, blocking version of this method.
 */
void
dbus_media_player2_player_call_seek (
    DbusMediaPlayer2Player *proxy,
    gint64 arg_Offset,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "Seek",
    g_variant_new ("(x)",
                   arg_Offset),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_media_player2_player_call_seek_finish:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_media_player2_player_call_seek().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_media_player2_player_call_seek().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_media_player2_player_call_seek_finish (
    DbusMediaPlayer2Player *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_media_player2_player_call_seek_sync:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @arg_Offset: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Seek">Seek()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_media_player2_player_call_seek() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_media_player2_player_call_seek_sync (
    DbusMediaPlayer2Player *proxy,
    gint64 arg_Offset,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "Seek",
    g_variant_new ("(x)",
                   arg_Offset),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_media_player2_player_call_set_position:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @arg_TrackId: Argument to pass with the method invocation.
 * @arg_Position: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.SetPosition">SetPosition()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_media_player2_player_call_set_position_finish() to get the result of the operation.
 *
 * See dbus_media_player2_player_call_set_position_sync() for the synchronous, blocking version of this method.
 */
void
dbus_media_player2_player_call_set_position (
    DbusMediaPlayer2Player *proxy,
    const gchar *arg_TrackId,
    gint64 arg_Position,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "SetPosition",
    g_variant_new ("(ox)",
                   arg_TrackId,
                   arg_Position),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_media_player2_player_call_set_position_finish:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_media_player2_player_call_set_position().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_media_player2_player_call_set_position().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_media_player2_player_call_set_position_finish (
    DbusMediaPlayer2Player *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_media_player2_player_call_set_position_sync:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @arg_TrackId: Argument to pass with the method invocation.
 * @arg_Position: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.SetPosition">SetPosition()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_media_player2_player_call_set_position() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_media_player2_player_call_set_position_sync (
    DbusMediaPlayer2Player *proxy,
    const gchar *arg_TrackId,
    gint64 arg_Position,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "SetPosition",
    g_variant_new ("(ox)",
                   arg_TrackId,
                   arg_Position),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_media_player2_player_call_open_uri:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @arg_Uri: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.OpenUri">OpenUri()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_media_player2_player_call_open_uri_finish() to get the result of the operation.
 *
 * See dbus_media_player2_player_call_open_uri_sync() for the synchronous, blocking version of this method.
 */
void
dbus_media_player2_player_call_open_uri (
    DbusMediaPlayer2Player *proxy,
    const gchar *arg_Uri,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "OpenUri",
    g_variant_new ("(s)",
                   arg_Uri),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * dbus_media_player2_player_call_open_uri_finish:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_media_player2_player_call_open_uri().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with dbus_media_player2_player_call_open_uri().
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_media_player2_player_call_open_uri_finish (
    DbusMediaPlayer2Player *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_media_player2_player_call_open_uri_sync:
 * @proxy: A #DbusMediaPlayer2PlayerProxy.
 * @arg_Uri: Argument to pass with the method invocation.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.OpenUri">OpenUri()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See dbus_media_player2_player_call_open_uri() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeeded, %FALSE if @error is set.
 */
gboolean
dbus_media_player2_player_call_open_uri_sync (
    DbusMediaPlayer2Player *proxy,
    const gchar *arg_Uri,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "OpenUri",
    g_variant_new ("(s)",
                   arg_Uri),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * dbus_media_player2_player_complete_next:
 * @object: A #DbusMediaPlayer2Player.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Next">Next()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_media_player2_player_complete_next (
    DbusMediaPlayer2Player *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * dbus_media_player2_player_complete_previous:
 * @object: A #DbusMediaPlayer2Player.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Previous">Previous()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_media_player2_player_complete_previous (
    DbusMediaPlayer2Player *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * dbus_media_player2_player_complete_pause:
 * @object: A #DbusMediaPlayer2Player.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Pause">Pause()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_media_player2_player_complete_pause (
    DbusMediaPlayer2Player *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * dbus_media_player2_player_complete_play_pause:
 * @object: A #DbusMediaPlayer2Player.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.PlayPause">PlayPause()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_media_player2_player_complete_play_pause (
    DbusMediaPlayer2Player *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * dbus_media_player2_player_complete_stop:
 * @object: A #DbusMediaPlayer2Player.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Stop">Stop()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_media_player2_player_complete_stop (
    DbusMediaPlayer2Player *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * dbus_media_player2_player_complete_play:
 * @object: A #DbusMediaPlayer2Player.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Play">Play()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_media_player2_player_complete_play (
    DbusMediaPlayer2Player *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * dbus_media_player2_player_complete_seek:
 * @object: A #DbusMediaPlayer2Player.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.Seek">Seek()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_media_player2_player_complete_seek (
    DbusMediaPlayer2Player *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * dbus_media_player2_player_complete_set_position:
 * @object: A #DbusMediaPlayer2Player.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.SetPosition">SetPosition()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_media_player2_player_complete_set_position (
    DbusMediaPlayer2Player *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * dbus_media_player2_player_complete_open_uri:
 * @object: A #DbusMediaPlayer2Player.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-mpris-MediaPlayer2-Player.OpenUri">OpenUri()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
dbus_media_player2_player_complete_open_uri (
    DbusMediaPlayer2Player *object G_GNUC_UNUSED,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * DbusMediaPlayer2PlayerProxy:
 *
 * The #DbusMediaPlayer2PlayerProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * DbusMediaPlayer2PlayerProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #DbusMediaPlayer2PlayerProxy.
 */

struct _DbusMediaPlayer2PlayerProxyPrivate
{
  GData *qdata;
};

static void dbus_media_player2_player_proxy_iface_init (DbusMediaPlayer2PlayerIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (DbusMediaPlayer2PlayerProxy, dbus_media_player2_player_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (DbusMediaPlayer2PlayerProxy)
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_MEDIA_PLAYER2_PLAYER, dbus_media_player2_player_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (DbusMediaPlayer2PlayerProxy, dbus_media_player2_player_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_MEDIA_PLAYER2_PLAYER, dbus_media_player2_player_proxy_iface_init))

#endif
static void
dbus_media_player2_player_proxy_finalize (GObject *object)
{
  DbusMediaPlayer2PlayerProxy *proxy = DBUS_MEDIA_PLAYER2_PLAYER_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (dbus_media_player2_player_proxy_parent_class)->finalize (object);
}

static void
dbus_media_player2_player_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 15);
  info = (const _ExtendedGDBusPropertyInfo *) _dbus_media_player2_player_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
dbus_media_player2_player_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface org.mpris.MediaPlayer2.Player: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
dbus_media_player2_player_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 15);
  info = (const _ExtendedGDBusPropertyInfo *) _dbus_media_player2_player_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.mpris.MediaPlayer2.Player", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) dbus_media_player2_player_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
dbus_media_player2_player_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_dbus_media_player2_player_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], DBUS_TYPE_MEDIA_PLAYER2_PLAYER);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, DBUS_TYPE_MEDIA_PLAYER2_PLAYER);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
dbus_media_player2_player_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  DbusMediaPlayer2PlayerProxy *proxy = DBUS_MEDIA_PLAYER2_PLAYER_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_media_player2_player_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_media_player2_player_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static const gchar *
dbus_media_player2_player_proxy_get_playback_status (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerProxy *proxy = DBUS_MEDIA_PLAYER2_PLAYER_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "PlaybackStatus");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
dbus_media_player2_player_proxy_get_loop_status (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerProxy *proxy = DBUS_MEDIA_PLAYER2_PLAYER_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "LoopStatus");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static gdouble 
dbus_media_player2_player_proxy_get_rate (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerProxy *proxy = DBUS_MEDIA_PLAYER2_PLAYER_PROXY (object);
  GVariant *variant;
  gdouble value = 0.0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Rate");
  if (variant != NULL)
    {
      value = g_variant_get_double (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
dbus_media_player2_player_proxy_get_shuffle (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerProxy *proxy = DBUS_MEDIA_PLAYER2_PLAYER_PROXY (object);
  GVariant *variant;
  gboolean value = FALSE;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Shuffle");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static GVariant *
dbus_media_player2_player_proxy_get_metadata (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerProxy *proxy = DBUS_MEDIA_PLAYER2_PLAYER_PROXY (object);
  GVariant *variant;
  GVariant *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Metadata");
  value = variant;
  if (variant != NULL)
    g_variant_unref (variant);
  return value;
}

static gdouble 
dbus_media_player2_player_proxy_get_volume (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerProxy *proxy = DBUS_MEDIA_PLAYER2_PLAYER_PROXY (object);
  GVariant *variant;
  gdouble value = 0.0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Volume");
  if (variant != NULL)
    {
      value = g_variant_get_double (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gint64 
dbus_media_player2_player_proxy_get_position (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerProxy *proxy = DBUS_MEDIA_PLAYER2_PLAYER_PROXY (object);
  GVariant *variant;
  gint64 value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "Position");
  if (variant != NULL)
    {
      value = g_variant_get_int64 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gdouble 
dbus_media_player2_player_proxy_get_minimum_rate (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerProxy *proxy = DBUS_MEDIA_PLAYER2_PLAYER_PROXY (object);
  GVariant *variant;
  gdouble value = 0.0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "MinimumRate");
  if (variant != NULL)
    {
      value = g_variant_get_double (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gdouble 
dbus_media_player2_player_proxy_get_maximum_rate (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerProxy *proxy = DBUS_MEDIA_PLAYER2_PLAYER_PROXY (object);
  GVariant *variant;
  gdouble value = 0.0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "MaximumRate");
  if (variant != NULL)
    {
      value = g_variant_get_double (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
dbus_media_player2_player_proxy_get_can_go_next (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerProxy *proxy = DBUS_MEDIA_PLAYER2_PLAYER_PROXY (object);
  GVariant *variant;
  gboolean value = FALSE;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "CanGoNext");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
dbus_media_player2_player_proxy_get_can_go_previous (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerProxy *proxy = DBUS_MEDIA_PLAYER2_PLAYER_PROXY (object);
  GVariant *variant;
  gboolean value = FALSE;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "CanGoPrevious");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
dbus_media_player2_player_proxy_get_can_play (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerProxy *proxy = DBUS_MEDIA_PLAYER2_PLAYER_PROXY (object);
  GVariant *variant;
  gboolean value = FALSE;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "CanPlay");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
dbus_media_player2_player_proxy_get_can_pause (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerProxy *proxy = DBUS_MEDIA_PLAYER2_PLAYER_PROXY (object);
  GVariant *variant;
  gboolean value = FALSE;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "CanPause");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
dbus_media_player2_player_proxy_get_can_seek (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerProxy *proxy = DBUS_MEDIA_PLAYER2_PLAYER_PROXY (object);
  GVariant *variant;
  gboolean value = FALSE;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "CanSeek");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
dbus_media_player2_player_proxy_get_can_control (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerProxy *proxy = DBUS_MEDIA_PLAYER2_PLAYER_PROXY (object);
  GVariant *variant;
  gboolean value = FALSE;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "CanControl");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static void
dbus_media_player2_player_proxy_init (DbusMediaPlayer2PlayerProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = dbus_media_player2_player_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, DBUS_TYPE_MEDIA_PLAYER2_PLAYER_PROXY, DbusMediaPlayer2PlayerProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), dbus_media_player2_player_interface_info ());
}

static void
dbus_media_player2_player_proxy_class_init (DbusMediaPlayer2PlayerProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = dbus_media_player2_player_proxy_finalize;
  gobject_class->get_property = dbus_media_player2_player_proxy_get_property;
  gobject_class->set_property = dbus_media_player2_player_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = dbus_media_player2_player_proxy_g_signal;
  proxy_class->g_properties_changed = dbus_media_player2_player_proxy_g_properties_changed;

  dbus_media_player2_player_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (DbusMediaPlayer2PlayerProxyPrivate));
#endif
}

static void
dbus_media_player2_player_proxy_iface_init (DbusMediaPlayer2PlayerIface *iface)
{
  iface->get_playback_status = dbus_media_player2_player_proxy_get_playback_status;
  iface->get_loop_status = dbus_media_player2_player_proxy_get_loop_status;
  iface->get_rate = dbus_media_player2_player_proxy_get_rate;
  iface->get_shuffle = dbus_media_player2_player_proxy_get_shuffle;
  iface->get_metadata = dbus_media_player2_player_proxy_get_metadata;
  iface->get_volume = dbus_media_player2_player_proxy_get_volume;
  iface->get_position = dbus_media_player2_player_proxy_get_position;
  iface->get_minimum_rate = dbus_media_player2_player_proxy_get_minimum_rate;
  iface->get_maximum_rate = dbus_media_player2_player_proxy_get_maximum_rate;
  iface->get_can_go_next = dbus_media_player2_player_proxy_get_can_go_next;
  iface->get_can_go_previous = dbus_media_player2_player_proxy_get_can_go_previous;
  iface->get_can_play = dbus_media_player2_player_proxy_get_can_play;
  iface->get_can_pause = dbus_media_player2_player_proxy_get_can_pause;
  iface->get_can_seek = dbus_media_player2_player_proxy_get_can_seek;
  iface->get_can_control = dbus_media_player2_player_proxy_get_can_control;
}

/**
 * dbus_media_player2_player_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2-Player.top_of_page">org.mpris.MediaPlayer2.Player</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_media_player2_player_proxy_new_finish() to get the result of the operation.
 *
 * See dbus_media_player2_player_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
dbus_media_player2_player_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (DBUS_TYPE_MEDIA_PLAYER2_PLAYER_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2.Player", NULL);
}

/**
 * dbus_media_player2_player_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_media_player2_player_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with dbus_media_player2_player_proxy_new().
 *
 * Returns: (transfer full) (type DbusMediaPlayer2PlayerProxy): The constructed proxy object or %NULL if @error is set.
 */
DbusMediaPlayer2Player *
dbus_media_player2_player_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return DBUS_MEDIA_PLAYER2_PLAYER (ret);
  else
    return NULL;
}

/**
 * dbus_media_player2_player_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2-Player.top_of_page">org.mpris.MediaPlayer2.Player</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See dbus_media_player2_player_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type DbusMediaPlayer2PlayerProxy): The constructed proxy object or %NULL if @error is set.
 */
DbusMediaPlayer2Player *
dbus_media_player2_player_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (DBUS_TYPE_MEDIA_PLAYER2_PLAYER_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2.Player", NULL);
  if (ret != NULL)
    return DBUS_MEDIA_PLAYER2_PLAYER (ret);
  else
    return NULL;
}


/**
 * dbus_media_player2_player_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like dbus_media_player2_player_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the thread-default main loop of the thread you are calling this method from (see g_main_context_push_thread_default()).
 * You can then call dbus_media_player2_player_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See dbus_media_player2_player_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
dbus_media_player2_player_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (DBUS_TYPE_MEDIA_PLAYER2_PLAYER_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2.Player", NULL);
}

/**
 * dbus_media_player2_player_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to dbus_media_player2_player_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with dbus_media_player2_player_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type DbusMediaPlayer2PlayerProxy): The constructed proxy object or %NULL if @error is set.
 */
DbusMediaPlayer2Player *
dbus_media_player2_player_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return DBUS_MEDIA_PLAYER2_PLAYER (ret);
  else
    return NULL;
}

/**
 * dbus_media_player2_player_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like dbus_media_player2_player_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See dbus_media_player2_player_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type DbusMediaPlayer2PlayerProxy): The constructed proxy object or %NULL if @error is set.
 */
DbusMediaPlayer2Player *
dbus_media_player2_player_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (DBUS_TYPE_MEDIA_PLAYER2_PLAYER_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.mpris.MediaPlayer2.Player", NULL);
  if (ret != NULL)
    return DBUS_MEDIA_PLAYER2_PLAYER (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * DbusMediaPlayer2PlayerSkeleton:
 *
 * The #DbusMediaPlayer2PlayerSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * DbusMediaPlayer2PlayerSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #DbusMediaPlayer2PlayerSkeleton.
 */

struct _DbusMediaPlayer2PlayerSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_dbus_media_player2_player_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], DBUS_TYPE_MEDIA_PLAYER2_PLAYER);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, DBUS_TYPE_MEDIA_PLAYER2_PLAYER);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_dbus_media_player2_player_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_media_player2_player_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_dbus_media_player2_player_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_dbus_media_player2_player_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _dbus_media_player2_player_skeleton_vtable =
{
  _dbus_media_player2_player_skeleton_handle_method_call,
  _dbus_media_player2_player_skeleton_handle_get_property,
  _dbus_media_player2_player_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
dbus_media_player2_player_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return dbus_media_player2_player_interface_info ();
}

static GDBusInterfaceVTable *
dbus_media_player2_player_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_dbus_media_player2_player_skeleton_vtable;
}

static GVariant *
dbus_media_player2_player_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_dbus_media_player2_player_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _dbus_media_player2_player_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _dbus_media_player2_player_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _dbus_media_player2_player_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.mpris.MediaPlayer2.Player", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _dbus_media_player2_player_emit_changed (gpointer user_data);

static void
dbus_media_player2_player_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _dbus_media_player2_player_emit_changed (skeleton);
}

static void
_dbus_media_player2_player_on_signal_seeked (
    DbusMediaPlayer2Player *object,
    gint64 arg_Position)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(x)",
                   arg_Position));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.mpris.MediaPlayer2.Player", "Seeked",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void dbus_media_player2_player_skeleton_iface_init (DbusMediaPlayer2PlayerIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (DbusMediaPlayer2PlayerSkeleton, dbus_media_player2_player_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (DbusMediaPlayer2PlayerSkeleton)
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_MEDIA_PLAYER2_PLAYER, dbus_media_player2_player_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (DbusMediaPlayer2PlayerSkeleton, dbus_media_player2_player_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (DBUS_TYPE_MEDIA_PLAYER2_PLAYER, dbus_media_player2_player_skeleton_iface_init))

#endif
static void
dbus_media_player2_player_skeleton_finalize (GObject *object)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (object);
  guint n;
  for (n = 0; n < 15; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (dbus_media_player2_player_skeleton_parent_class)->finalize (object);
}

static void
dbus_media_player2_player_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 15);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_dbus_media_player2_player_emit_changed (gpointer user_data)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.mpris.MediaPlayer2.Player",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_dbus_media_player2_player_schedule_emit_changed (DbusMediaPlayer2PlayerSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
dbus_media_player2_player_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _dbus_media_player2_player_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _dbus_media_player2_player_emit_changed");
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
dbus_media_player2_player_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  const _ExtendedGDBusPropertyInfo *info;
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 15);
  info = (const _ExtendedGDBusPropertyInfo *) _dbus_media_player2_player_property_info_pointers[prop_id - 1];
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL &&
          info->emits_changed_signal)
        _dbus_media_player2_player_schedule_emit_changed (skeleton, info, prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
dbus_media_player2_player_skeleton_init (DbusMediaPlayer2PlayerSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = dbus_media_player2_player_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, DBUS_TYPE_MEDIA_PLAYER2_PLAYER_SKELETON, DbusMediaPlayer2PlayerSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 15);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_DOUBLE);
  g_value_init (&skeleton->priv->properties[3], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[4], G_TYPE_VARIANT);
  g_value_init (&skeleton->priv->properties[5], G_TYPE_DOUBLE);
  g_value_init (&skeleton->priv->properties[6], G_TYPE_INT64);
  g_value_init (&skeleton->priv->properties[7], G_TYPE_DOUBLE);
  g_value_init (&skeleton->priv->properties[8], G_TYPE_DOUBLE);
  g_value_init (&skeleton->priv->properties[9], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[10], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[11], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[12], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[13], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[14], G_TYPE_BOOLEAN);
}

static const gchar *
dbus_media_player2_player_skeleton_get_playback_status (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_string (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
dbus_media_player2_player_skeleton_get_loop_status (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_string (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gdouble 
dbus_media_player2_player_skeleton_get_rate (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (object);
  gdouble value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_double (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
dbus_media_player2_player_skeleton_get_shuffle (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_boolean (&(skeleton->priv->properties[3]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static GVariant *
dbus_media_player2_player_skeleton_get_metadata (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (object);
  GVariant *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_variant (&(skeleton->priv->properties[4]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gdouble 
dbus_media_player2_player_skeleton_get_volume (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (object);
  gdouble value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_double (&(skeleton->priv->properties[5]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gint64 
dbus_media_player2_player_skeleton_get_position (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (object);
  gint64 value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_int64 (&(skeleton->priv->properties[6]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gdouble 
dbus_media_player2_player_skeleton_get_minimum_rate (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (object);
  gdouble value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_double (&(skeleton->priv->properties[7]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gdouble 
dbus_media_player2_player_skeleton_get_maximum_rate (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (object);
  gdouble value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_double (&(skeleton->priv->properties[8]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
dbus_media_player2_player_skeleton_get_can_go_next (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_boolean (&(skeleton->priv->properties[9]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
dbus_media_player2_player_skeleton_get_can_go_previous (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_boolean (&(skeleton->priv->properties[10]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
dbus_media_player2_player_skeleton_get_can_play (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_boolean (&(skeleton->priv->properties[11]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
dbus_media_player2_player_skeleton_get_can_pause (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_boolean (&(skeleton->priv->properties[12]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
dbus_media_player2_player_skeleton_get_can_seek (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_boolean (&(skeleton->priv->properties[13]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
dbus_media_player2_player_skeleton_get_can_control (DbusMediaPlayer2Player *object)
{
  DbusMediaPlayer2PlayerSkeleton *skeleton = DBUS_MEDIA_PLAYER2_PLAYER_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_marshal_value_peek_boolean (&(skeleton->priv->properties[14]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
dbus_media_player2_player_skeleton_class_init (DbusMediaPlayer2PlayerSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = dbus_media_player2_player_skeleton_finalize;
  gobject_class->get_property = dbus_media_player2_player_skeleton_get_property;
  gobject_class->set_property = dbus_media_player2_player_skeleton_set_property;
  gobject_class->notify       = dbus_media_player2_player_skeleton_notify;


  dbus_media_player2_player_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = dbus_media_player2_player_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = dbus_media_player2_player_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = dbus_media_player2_player_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = dbus_media_player2_player_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (DbusMediaPlayer2PlayerSkeletonPrivate));
#endif
}

static void
dbus_media_player2_player_skeleton_iface_init (DbusMediaPlayer2PlayerIface *iface)
{
  iface->seeked = _dbus_media_player2_player_on_signal_seeked;
  iface->get_playback_status = dbus_media_player2_player_skeleton_get_playback_status;
  iface->get_loop_status = dbus_media_player2_player_skeleton_get_loop_status;
  iface->get_rate = dbus_media_player2_player_skeleton_get_rate;
  iface->get_shuffle = dbus_media_player2_player_skeleton_get_shuffle;
  iface->get_metadata = dbus_media_player2_player_skeleton_get_metadata;
  iface->get_volume = dbus_media_player2_player_skeleton_get_volume;
  iface->get_position = dbus_media_player2_player_skeleton_get_position;
  iface->get_minimum_rate = dbus_media_player2_player_skeleton_get_minimum_rate;
  iface->get_maximum_rate = dbus_media_player2_player_skeleton_get_maximum_rate;
  iface->get_can_go_next = dbus_media_player2_player_skeleton_get_can_go_next;
  iface->get_can_go_previous = dbus_media_player2_player_skeleton_get_can_go_previous;
  iface->get_can_play = dbus_media_player2_player_skeleton_get_can_play;
  iface->get_can_pause = dbus_media_player2_player_skeleton_get_can_pause;
  iface->get_can_seek = dbus_media_player2_player_skeleton_get_can_seek;
  iface->get_can_control = dbus_media_player2_player_skeleton_get_can_control;
}

/**
 * dbus_media_player2_player_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-mpris-MediaPlayer2-Player.top_of_page">org.mpris.MediaPlayer2.Player</link>.
 *
 * Returns: (transfer full) (type DbusMediaPlayer2PlayerSkeleton): The skeleton object.
 */
DbusMediaPlayer2Player *
dbus_media_player2_player_skeleton_new (void)
{
  return DBUS_MEDIA_PLAYER2_PLAYER (g_object_new (DBUS_TYPE_MEDIA_PLAYER2_PLAYER_SKELETON, NULL));
}

